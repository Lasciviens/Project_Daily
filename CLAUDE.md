# Lasci's Board — Master Project Guide

> Read this before touching any code. Single source of truth for product/feature state and coding rules.
> Keep this file lean — no clutter, no outdated info, no verbose explanations. Every line must earn its place.

**Doc map** — read in this order depending on what you're doing:
- `README.md` (repo root) — human/AI-agnostic orientation: what this app is, tech overview, quick start. Start here if you've never seen this repo.
- **`CLAUDE.md` (this file)** — the definitive guide for any coding agent (Claude or otherwise) before making a change: routes, features, coding rules, edge functions, key patterns.
- `AGENTS.md` (repo root) — supplementary rules specific to database/schema/Supabase work (migrations, RLS, edge function conventions).
- `docs/` — indexed in `docs/README.md`, in three kinds: **runbooks** (`iphone-examples.md`, `web-push-setup.md`, `scriptable-food-logger.md`, `scriptable-widgets.md`, `health-auto-export/`) that are authoritative for one-time setup steps and must be followed exactly; **live coordination** (`coord/`, `codex-shortcuts.md`) that must be read fresh every session; and **verified reference** (`google-health-api-surface.md`, `fitbit-air-integration.md`, `ai-cost-capability-analysis.md`, `iphone-web-integration.md`, the EnTur Postman collection). For feature/architecture state this file still wins; the runbooks and the coordination logs are **not** "secondary".
- **Backlog** = the in-app Dev Requests drawer (`dev_requests` table), not a markdown list. This file's "Not done yet" is a coarse long-term list only.
- **Cross-AI task board** = `docs/codex-shortcuts.md`.

---

## Session Workflow

Runs on **Claude Code on the web** (claude.ai/code). Container clones repo fresh each session — git is the only memory.

```
1. User describes task
2. Claude branches: claude/<descriptive-name>
3. Code → npm run build → commit → push → draft PR
4. User marks ready → merges → GitHub Actions auto-deploys (~1 min)
```

- Never push directly to `main`
- Update this file when features complete or architecture changes
- **Small data migrations/inputs**: the in-app AI (Ask AI panel) has a generic DB layer (`db_query/insert/update/delete`, see AI section). For small one-off data loads (seed rows, backlog items, bulk inserts) just hand it a JSON or SQL-ish spec in chat — it inserts the rows — instead of writing a migration. Reserve SQL migrations for schema changes.
- **Direct Supabase data access (curl/REST with the user's own credentials) is opt-in per request** — only query/inspect live production data when the user explicitly asks for it in that turn (e.g. "check the DB", "look at my data"). Never do this proactively/unprompted, even mid-investigation of a bug — it's the user's personal data.
- **No unit-test framework exists in this repo** (`package.json` ships only `@playwright/test`). Logic verification ships as a throwaway `scripts/*.cjs|.mjs` run through `sucrase` against the real modules — precedent: `scripts/verify-health-source-resolver.cjs` (25 assertions), `scripts/generate-matvaretabellen-seed.mjs`. **Do not introduce vitest/jest as a side effect of an unrelated change.**
- **Dev-server gotcha:** an old vite process holding `:5173` serves a STALE Tailwind config (new plugins silently missing). Kill all vite processes; don't trust the port.

**Live URLs:** App: `https://lasciviens.github.io/Project_Daily/#/login` · Repo: `https://github.com/Lasciviens/Project_Daily`

---

## Two-AI setup (Claude + Codex)

Two AIs work this repo. **Ownership is exclusive — never edit outside your side.**

- **Claude** = manager: `src/**`, `supabase/**` (functions + migrations), all `docs/**` except the generator readme, PR review.
- **Codex** = owner of **`scripts/iphone-shortcuts/**` and nothing else** (the Apple Shortcuts generator + its README).
- **Durable spec + task board:** `docs/codex-shortcuts.md` (roles, the 6 rules, C-numbered tasks with statuses). Not a conversation.
- **Messages:** two append-only logs — `docs/coord/to-codex.md` (Claude → Codex) and `docs/coord/to-claude.md` (Codex → Claude). Newest entry on TOP, header `### YYYY-MM-DD HH:MM · <task id> · re: <prior|new>`. **Never edit the other side's log**; a new entry in the other log with `re:` pointing at yours IS the reply. Protocol: `docs/coord/README.md`.
- **The one sanctioned exception to the English-only rule:** Turkish is allowed inside generated **on-phone user-facing strings** (Shortcut/widget titles, notification text). Never in prose, comments, commits or docs.

---

## Communication Style (per user request)

The user is learning software engineering terminology as we go (comes from a SAP/ABAP background). When describing what was done, **use the correct technical term and add a short parenthetical explaining what it means** — e.g. "yeni bir **hook** yazdım (component'ler arası paylaşılan, tekrar kullanılabilir mantık — SAP'deki bir class method'una en yakın karşılık)" instead of just describing it in plain words with no term attached. Do this consistently, not just when explicitly asked — it's meant to build vocabulary over many sessions. Don't overdo it into a glossary dump; one clear parenthetical per term, only for terms actually used that turn.

Also: don't narrate routine PR housekeeping (subscribing to PR activity, "checking CI status", "no comments yet") — the user checks PRs themselves and considers this noise. Only speak up about a PR when there's something requiring their input or a genuinely notable outcome (merged, a real CI failure, a review comment needing a decision).

---

## Stack

| Layer | Choice |
|---|---|
| Framework | React 19 + TypeScript 6 + Vite 8 |
| Routing | `react-router-dom` v7, HashRouter (`/#/route`) — GitHub Pages has no server rewrite |
| Styling | Tailwind CSS v3 (+ `@tailwindcss/container-queries`), design tokens via CSS custom properties (`--accent-*`) |
| State (UI) | Zustand (`src/app/store.ts`) |
| State (server) | TanStack Query v5 |
| DB | Supabase (Postgres + Auth + RLS + Edge Functions) |
| Hosting | GitHub Pages via GitHub Actions |
| E2E / verification | `@playwright/test` (no unit-test framework — see Session Workflow) |

UI primitives: `@headlessui/react` v2 (Dialog, Combobox, Popover, Menu). Charts: `recharts`. No animation library, no shadcn/ui, no SSR, no AI keys in client code.

---

## Platform & Shell

**PWA / service worker (real bug, fixed):** `vite-plugin-pwa`'s `registerType: 'autoUpdate'` does NOT auto-update the way the name implies — the auto-injected `registerSW.js` is a bare `navigator.serviceWorker.register(...)` with no update-detection logic. A new service worker installs but sits **waiting** until every open tab is fully closed; a hard refresh does NOT activate it, because a hard refresh bypasses the HTTP cache, not an already-controlling service worker. A shipped fix was invisible to the user even after a successful deploy + hard refresh. Fixed: `vite.config.ts` sets `injectRegister: false` + `workbox: { skipWaiting: true, clientsClaim: true }`, and `src/main.tsx` calls `registerSW({ immediate: true })` from `virtual:pwa-register` (types in `src/vite-env.d.ts`) — checks for updates immediately (and periodically) and reloads automatically, so deploys actually reach the browser.

**Mobile app-shell** (each bullet is a device-verified gotcha):
- **The document NEVER scrolls** — `html/body/#root` are fixed-height (index.css) and `<main>` (layout.tsx) is THE scroll container. This is what made custom pull-to-refresh reliable on a real iPhone: with the document as scroller, iOS decides gesture ownership on the FIRST touchmove — one uncancelled move commits the root rubber-band and flips `e.cancelable` false for the rest of the gesture, so the pull only worked when the first move happened to be cleanly downward.
- `usePullToRefresh` has a slop + axis-lock state machine (horizontal swipes on snap strips are never hijacked; pull distance re-anchors at commit) and an `isReady` state (indicator flips to solid accent = "release to refresh").
- Route changes reset `main.scrollTop` (useLayoutEffect in Layout, pre-paint so view-transition snapshots start at top).
- **Tab navigation slides directionally:** BottomTabBar passes `forward`/`back` (tab order) → `useViewTransitionNav` stamps `data-vt-dir` on `<html>` → slide keyframes in index.css; header/tab-bar have their own `view-transition-name` so the bars stay put while content slides.
- **View Transitions don't exist on pre-18 iOS Safari** — there, Layout keys the Outlet wrapper by pathname and plays a `.page-in` rise (universal fallback, else navigation is INSTANT = "animations aren't there" on the primary device); with VT support the key stays constant (no double animation). That wrapper is `h-full` ONLY on /daily|/shop|/recipes — everywhere else it must stay auto-height or tall pages overflow the fixed box and swallow main's bottom padding (last card pinned under the tab bar; real regression, caught by E2E).
- **ALL view-transition CSS lives OUTSIDE `@layer`** — same Tailwind purge gotcha as the recharts rules; the original crossfade rule inside `@layer base` was silently purged from every build.
- `.stagger-in` (mobile-only ≤639px) cascades a container's children in with 30ms-stepped delays — HomePage columns, TodaySummary grid, MediaPage main column.
- **BottomTabBar is a floating glass capsule** (left-3 right-3, rounded-[26px], safe-area offset, active icon in an accent chip) — `<main>` reserves `pb-[calc(5.5rem+env(safe-area-inset-bottom))]`.
- **`.glass-chrome`** (index.css, `@layer components` — safe there because the class name appears in source, unlike runtime-only selectors) gives the fixed header + tab bar iOS "liquid glass": translucent `cream-50/0.62` + `backdrop-filter: blur(20px) saturate(180%)` (the saturate is what reads as real glass, not a plain blur) + an inset top hairline; theme-aware dark override + an `@supports not` near-opaque fallback. **Write only the standard `backdrop-filter`** — autoprefixer emits the `-webkit-` Safari needs; hand-writing both made the minifier drop the standard one. The glass frosts because `<main>` scrolls BEHIND the fixed chrome.
- **iOS input auto-zoom is disabled app-wide** (real bug: tapping the AI chat box zoomed the page and the zoom stuck after blur, misaligning the frame — iOS zooms whenever a focused text control's font-size is <16px): `maximum-scale=1` in the viewport meta (pinch-zoom still works — iOS ignores max-scale for pinch since iOS 10) + a `@media (pointer: coarse)` rule forcing all text-entry controls to `font-size: 16px !important`. This removes the trigger itself — never "fix" this per-field.
- Header gains a shadow once `main.scrollTop > 8`; AIPanel's mobile sheet has a grab handle + `rounded-t-3xl`. Touch polish: `-webkit-tap-highlight-color: transparent` + `touch-action: manipulation`, `:active` scale in `.btn-*`/`.card-interactive`, `animate-tabPop` on the active tab.
- iOS PWA is edge-to-edge (`black-translucent` status bar + safe-area top padding on the header); `theme-color`/manifest colors match the cream design. Pages sizing to the viewport use `min-h-full` — never `min-h-[calc(100vh-56px)]`.
- **CommandBar (⌘K) has a mobile trigger**: the header Search button is icon-only (🔍) below `md` (was `hidden md:flex` — the palette was unreachable on phones since ⌘K can't be typed).

**iOS platform limits — hard constraints for anything "while the app is closed"** (full rationale: `docs/iphone-web-integration.md`):
- **No background execution at all.** PWAs get no Background/Periodic Sync; Shortcuts has no daemon. Unattended runs need a trigger (time/location/NFC) and **iOS forces a visible banner on every auto-run automation**. Server→phone delivery must go through Web Push.
- **iOS Web Push:** home-screen-**installed PWA only** (never a Safari tab), iOS 16.4+, direct-tap permission grant, and **every push must display a visible notification** (no silent data push). Delivery is best-effort and may be minutes late.
- **Web Share Target is unsupported on iOS** — "share a photo into the app to log it" would need a native Share Extension (a real App Store app).
- **Scriptable widgets:** refresh budget ≈40–70/day, ≥5 min apart, iOS decides the real cadence; a **Small widget has ONE tap target** and cannot run code in place — a tap opens a deep link (the `HizliLog` runner pattern).

---

## Routes

```
/#/login          → LoginPage (public)
/#/reset-password → ResetPasswordPage (public — reachable only via Supabase's recovery email link or LoginPage's "Forgot password?")
/#/home       → HomePage
/#/daily      → DailyPage    (nav: "Personal" — Daily ONLY, no in-header group tabs)
/#/recipes    → RecipesPage  (nav: "Food" — top-level entry, renders FoodTabs)
/#/shop       → ShopPage     (reached from the Food nav entry, renders FoodTabs)
/#/media      → MediaPage
/#/work       → WorkPage
/#/projects   → ProjectsPage
/#/training   → TrainingPage
/#/games      → GamesPage
/#/wishes     → WishesPage   (reached from the mobile More sheet — the 5 bottom-bar slots are full — and the desktop nav row)
/#/developer  → DeveloperPage (reached from the Settings ⚙ menu)
```

Everything except `/login` and `/reset-password` is protected by `SessionGuard` in `src/app/router.tsx`. No `/football` route — Football is deferred (see Features).

**Nav grouping** (all in `src/app/layout.tsx`; routes are stable so deep links never break): desktop order Home · Personal · Food · Media · Work · Training · Projects · Games · Wishes. Mobile bottom bar = 5 primary tabs (Home · Personal · **Food**, `UtensilsCrossed` · Media · Training) + More; **Wishes leads the More sheet** (a wish list that has to be hunted for is dead in three weeks). From `sm:` up that desktop row is the ONLY nav — `BottomTabBar` is `sm:hidden` — so anything living in the More sheet needs a link there too or it is unreachable on desktop. `PersonalLayout.tsx` is a scroll shell that exports **`FoodTabs`** (Food | Shop), rendered inside the Food and Shop pages' first header row; Daily renders no group tabs. (There is no `PersonalTabs` component and no in-header tab bar on /daily.)

**Password reset (real bug, fixed):** the app had no recovery flow — Supabase's reset email redirected back with a recovery token, but nothing read it or showed a "set new password" form, so the link silently did nothing (a very common gap — see the `gotrue-js`/`auth-js` issues about apps not handling `PASSWORD_RECOVERY`). Fixed: LoginPage's "Forgot password?" → `requestPasswordReset()` (`security/supabaseClient.ts`) calls `supabase.auth.resetPasswordForEmail(email, { redirectTo })` pointing at `#/reset-password`; `ResetPasswordPage` listens for the `PASSWORD_RECOVERY` auth event (plus a session-check fallback in case the event fires before the listener attaches). **One-time manual step:** the redirect URL (GitHub Pages URL + `http://localhost:5173/#/reset-password`) must be added to Supabase Dashboard → Authentication → URL Configuration → Redirect URLs, or Supabase silently refuses to send the token.

---

## Features

| Feature | Status | Notes |
|---|---|---|
| Auth | ✅ | LoginPage + password recovery (`/reset-password`, see Routes) |
| Daily + To-Do | ✅ | See Daily section below. Four modes (Day/Week/Month/Tasks) + a glance board. |
| Food | ✅ | See Food section below. Diary + plan in one table, coaching, barcode/online search, water. Nav: top-level "Food" (`/recipes`), holds Shop. |
| Shop | ✅ | See Shop section below. Reached from the Food nav entry (`FoodTabs`). |
| Media | ✅ | See Media section below |
| Work | ✅ | **Command-center layout**: sticky command bar (live HH:MM:SS clock, day stat chips ✓/⚡/⚠, New task, rail toggle) → `WorkDayTimeline` (with a Now/Next line) → `FocusStrip` (focused tasks, horizontal snap-scroll) → collapsible red **Overdue alert strip** (overdue is a property, not a column — except in_progress tasks, which stay on the board) → **Board/List toggle** (persisted) + quick-add + search + priority filter → `WorkBoard` (true horizontal 4-col kanban on desktop w/ per-column scroll + DnD; stacked collapsible on mobile) or `WorkListView` (dense triage rows, status-cycle dot, overdue-first sort). Right rail (`WorkSidebar`, collapsible+persisted): Summary/Notes/Goals/Links as stacked collapsible cards. Shared status/priority/due helpers in `components/workMeta.ts`. |
| AI | ✅ | See AI section below. Gemini 3.5 Flash (+3-model fallback) via `ai-proxy`; generic DB tool layer, vision, semantic search, aggregation, memory. |
| Calendar | ✅ | Google OAuth (unified consent, see `calendar-oauth`), read + write events. Sync/refresh button in the Daily agenda header. Two-way block↔event link — see Key Patterns. |
| Games | ✅ | RP5 library proxy, 6 view modes, TierEditor, PlayQueue drag-and-drop |
| Training | ✅ | See Training section below. Hevy + Strava + Apple Health (Health Auto Export) + Fitbit Air (Google Health) + AI PT Coach. |
| Projects | ✅ | Phases, items, status tracking. Add/edit share one modal (`ProjectItemModal`) — title/notes/phase/type/status/priority. Phases view is a matrix (`grid grid-cols-1 md:grid-cols-2`, each card sized/collapsed independently), not one stacked column. |
| Wishes | ✅ | See Wishes section below. `wish_items` (migration `069`) — things to do and places to go, scoped to a **reminder period that is never a deadline**. `/wishes` + a resurfacing row in Daily's `DayView` + a once-per-season `push-send` segment. |
| iPhone surface | ✅ | See iPhone section below. `phone-gateway` (11 actions) + 11 Apple Shortcuts + a Scriptable food logger + 4 widgets. |
| Web Push | ✅ | See iPhone section below. Migration `068` + `push-send` + pg_cron morning brief + `public/push-sw.js`; toggle in Settings (⚙) → Notifications. |
| Dev Requests | ✅ | **The live backlog.** Right-edge drawer trigger in `Nav`; jot bugs/features/improvements/integration ideas/long-term wishes about the app itself so a future session reads a structured backlog instead of chat scrollback. `dev_requests` (migration `042`, owner-only RLS, `user_id DEFAULT auth.uid()`): title, description, `page` (auto-captured route, editable), `category` (bug/feature/improvement/integration/longterm/question/other), `priority`, `status` (open/in_progress/done/dismissed), `effort`, `sort_order`. `DevRequestsDrawer.tsx`: quick-add prefilled with the current page, category pills + manual/priority sort, HTML5 drag-reorder (optimistic `onMutate`), inline edit-in-place, status cycles open→in_progress→done on click, delete for done/dismissed. Replaced the old global To-Do drawer (`ToDoDrawer`/`ToDoSection` deleted; `ToDoItem` + task hooks untouched). **Known side effect:** `useSyncFromGoogleTasks`/`usePushToGoogleTasks` (bulk Google Tasks import/push) lost their only UI trigger and are currently unreachable from any page — left defined, harmless. |
| Developer | ✅ | Standalone `/developer`, reached from Settings (⚙). Tabs: **Activity** (CRUD audit trail — `audit_logs`, written by DB triggers, migration `037`; filters by table/op/actor/days, 30-day retention swept probabilistically inside the trigger) and **Errors** (`app_error_logs`), plus a "Reindex AI search" button. Audit triggers cover user-authored tables only — bulk-synced tables (hevy_*/health_*/strava) are deliberately excluded to avoid sync-spam; `actor` distinguishes 'web' (browser session) from 'service' (AI/webhooks, service-role writes). Activity is a **4-column matrix** (`grid-cols-4` wide) rather than a vertical list: a same-transaction cascade (e.g. a `time_blocks` delete that migration 043's trigger cascades into a `tasks` delete) spans columns as one chain with "→" arrows, so "this caused that" reads visually. |
| Home | ✅ | See Home section below. Daily briefing, weather, transit, currency, news, media, games, training, projects, glance board. |
| Command Bar | ✅ | `CommandBar` (⌘K) via `useUIStore.openCommandBar` |
| Dark Mode | ✅ | Light/Dark/System in `SettingsMenu.tsx` → `useThemeStore` (Zustand + persist, key `theme-preference`). `tailwind.config.js` (`darkMode: 'selector'`) resolves `canvas`/`cream`/`ink` through `rgb(var(--x) / <alpha-value>)` (`accent` was already CSS-var-backed) — `index.css` defines light values (`:root`) and dark overrides (`:root.dark`) once, so ~120 component files using those token classes became theme-aware with **zero per-file changes**. An inline script in `index.html` (before the module script) stamps `.dark` synchronously from the persisted value (no flash-of-wrong-theme); a `matchMedia` listener keeps `'system'` live. `bg-white` (opaque surface color, ~120 files) was mechanically renamed to `bg-cream-50`; literal `text-white` / `bg-white/NN` / `bg-black` were deliberately left alone (foreground-on-accent and photo-overlay roles). Chart chrome (grid lines, axis labels) moved to `rgb(var(--ink-XXX))`; **distinct data-series colors** (sleep stages, Strava orange, task color tags, Activity Rings) stay fixed literals — identity colors, not UI chrome. |
| Football | ⛔ | Deferred — no route wired, and the `football-api` edge function is no longer in the repo. API-Football free tier stops at 2024. Future: fixtures from Google Calendar. |

### Daily Feature Detail

`DailyPage.tsx` (`src/features/daily/pages/`) — ONE compact header row: shared `DateNav` (‹ date ›, size md) + context label, then period tabs far right. **Four modes** (`type Mode = 'day' | 'week' | 'month' | 'tasks'`); the Yesterday/Today/Tomorrow highlight is DERIVED from `viewDate` (the three old near-identical day views are one `DaySection`). Desktop tabs: Yesterday · Today · Tomorrow · Week · Month · Tasks; mobile collapses to a 4-cell segmented control (Today · Week · Month · Tasks).

- **Day mode = two stacked bands.** Band 1 (schedule hero, `xl:grid-cols-[minmax(0,60rem)_minmax(0,1fr)]`): `WeekStrip` (7 fixed day cells for the viewed week, chevrons browse without changing viewDate, cell click = view that day; the full `WeekWidget` lives under the Week tab) → `DayAgenda bare` + `DayView` side by side at `lg:` (`lg:grid-cols-[minmax(0,34rem)_minmax(0,1fr)]`, hairline `divide-x`), plus a **`DayQuickRail`** companion rail at `xl+` (quick actions / next-up / an entry into Tasks). Band 2 = full-width `TodaySummary`.
- **`TodaySummary` (glance board)** — shown for EVERY viewed day, not just today. `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 2xl:grid-cols-4 gap-3`; each module is its own `rounded-2xl border border-ink-200 bg-cream-50 shadow-card` card, and **Nutrition takes a double slot** (`sm:col-span-2`). **NO auto-fill anywhere and explicit column counts only** — the user requires boxes never to change position based on content/size (verified by E2E: cell x/w identical between a full and an empty day). Cells share `summary/cellKit.tsx` anatomy (Cell / CellHeader icon-chip / CellLink — link-outs are ink, accent is budgeted for primary actions). An empty module collapses to a compact row IN PLACE; it never leaves its slot. Cards: **NutritionCard** (double slot, see below), **TrainingCard** (planned/done; on a rest day it lists Hevy routines INLINE and plans one via `UnifiedPlanModal` prefilled for the viewed date), **WatchNextCard** (chip-switch between watching/paused series, real next-unwatched episode from `useNextEpisode`, SxxEyy + title + watched count + future-air-date badge, ✓ Watched auto-advances, 📅 Plan stamps an episode-linked time block), **HealthCard** (swipeable sleep/steps/energy/heart/weight strip reusing the Health aggregation), **GamesCard**, **ShopCard**. Work + Projects cards were removed (they have their own pages).
- **`NutritionCard` = Nutrition + Meals merged into one card** (the separate `summary/MealsCard.tsx` was deleted). It carries: the meal timeline (slots 🌅 Breakfast · ☀️ Lunch · 🌙 Dinner · 🍎 Snack · 💊 Suppl., current-slot highlighted on today) with per-row ✎ edit / ✓ confirm-planned / 🗑 delete, per-slot **free-text quick-add** (live data showed usage is ~100% custom titles; an exact/start-of-name library match auto-logs with real macros, a loose match opens `FoodLogModal` prefilled), recent-food chips (`useRecentFoods`, diary-sourced, re-log their own snapshot), ⧉ copy-yesterday into empty slots, the calorie ring + protein/macros, and **`WaterTracker`**.
- **`DayAgenda`** replaced the old 24h×52px pixel-grid `DayTimeline` (deleted): dense agenda rows (time · duration · title · ⟳ recurring · ◈ calendar · ⚠ overlap badges; +30m/+1d/rename/delete actions), 🌙 Night group for blocks before 06:00, clickable "Nh free · +" gap rows, a red now-marker, NULL-`start_time` blocks in a "No time set" group (real data has them), empty-day quick-add chips (Morning/Afternoon/Evening). Same data sources/mutations as before; drag-to-reschedule was dropped with the grid.
- **Week mode** = `WeekWidget`. **Month mode** = two panes: a bigger calendar (`MonthWidget big`) + the picked day's editable `DayAgenda` shown IN PLACE (selecting a day does NOT navigate away); with no day picked the right pane lists Upcoming dated activities. **Tasks mode** = `TasksPanel`, an aggregated all-tasks view (shipped from a dev request).
- **Cancel** (⊘, `ToDoItem`/`WorkTaskCard`) sets `status='cancelled'` — `TaskStatus` always had the value but no UI ever set it; distinct from Delete (keeps a record instead of destroying the row). `DayView` has a "Cancelled" section (same 24h window as "Done") so a cancelled task stays visible; the Work board has no cancelled column, so there it simply drops off (matches the "cancelled excluded from active counts" convention used everywhere — WeekWidget/MonthWidget/HomePage/…).
- **The `section` day-leak (real bug, fixed on the READ side only).** `useDayData` maps a whole RANGE of day-offsets onto ONE `section` value (+2…+7 → `this_week`, everything else → `backlog`), and `fetchTasksForDay`'s non-today arm carried `due_date.is.null` — so **one** undated `this_week` row rendered on all six of those days, and **one** undated `backlog` row claimed yesterday *and* every far-future day. The entire fix is one clause: only `section === 'today'` keeps `due_date.is.null,due_date.lte.X` (an undated row parked in Today IS today's work, and an open overdue task must still surface there); every other day view is now `due_date.eq.X` exactly. **No row was rewritten and `section` was not dropped** — a drop is a 4-unit break (`037`'s audit list, `046`'s policy list, `DB_CATALOG`) for zero gain, and **both backfills are verifiably worse than nothing**: `→'backlog'` relocates rows onto an unbounded date range (the same bug, wider), `→due_date=today` manufactures deadlines that go **red tomorrow** out of rows that were never deadlines. Nothing lost its home: undated rows still reach `fetchAllTasks` → `TasksPanel`'s "No date" group, and undated `this_week` rows still reach `fetchTasksByWeek`'s untouched section arm → `DayView`'s "This week — no date" band. `TaskTab`'s `SECTIONS` also dropped the `Tomorrow`/`This Week` pills (neither has a home of its own — picking one only made a task float like Backlog under a date-shaped name; a row already holding one keeps a "(legacy)" pill so the value stays visible and one tap moves it off). **`planForm.ts`'s `section: 'today'` default must NOT be changed** — four server-side `section.eq.today` predicates (`phone-gateway` ×2, `push-send`, `ai-proxy`) drain silently if it becomes `'inbox'`, quietening the brief and the lock-screen push forever.

### Wishes Feature Detail

`/#/wishes` → `WishesPage` (`src/features/wishes/`). A **wish** is something you want to do, or a place you want to go — "go to the hytte this winter", "things to do in Polatlı" — carrying a **reminder period**. The period decides only when the app brings the wish up first; **it is never a deadline and never a visibility rule, so a wish is never late** (see Coding Rules → `NEVER_HIDES`).

- **Its own table — `wish_items`** (migration `069`: owner-only RLS, `trg_audit`, `trg_wish_items_updated_at → update_updated_at()`, `(user_id, period_start)` index): title, notes, `kind` `thing|place`, `period_start`/`period_end`, `period_label` (the user's own word for the window), `city`/`country`/`url` (meaningful when `kind='place'` — the same one-table variant shape `shop_items` uses for its own), `priority`, `status` `idea|planned|done|dropped`, `promoted_task_id`, `sort_order`. Periods are **concrete dates**: no MM-DD, no season enum (a 5-value CHECK can't say "*this* spring, Italy", its `'anytime'` default IS a junk drawer, and CHECK values are duplicated into `ai-proxy`'s catalog string → a redeploy per new period word), and **no generated `is_open` column ever** — `CURRENT_DATE` is STABLE, not IMMUTABLE, so Postgres rejects it in a generation expression. Migration 069 **updates not one existing row**.
- **Not `tasks`:** every one of the five `section` values leaks. An undated row matches `fetchTasksForDay`'s `due_date.is.null` arm and `fetchTasksByWeek`'s unbounded `.eq('section','this_week')` arm, and four server-side `section.eq.today` predicates would put "hytte this winter" on the **lock screen in July**. The obvious repair — a `kind` discriminator filtered OUT of those queries — is forbidden twice: it removes rows (`NEVER_HIDES`), and `ai-proxy`'s `db_query` composes **model-supplied** filters over `select('*')`, so no server-side code can enforce it anyway. **Not `shop_items`:** `category_id` is `NOT NULL` and Daily's `ShopCard` filters `status === 'wishlist'` with **no** category filter, so a place-wish would advertise itself in the Shopping glance cell. Buy vs do: "buy ski gear this winter" is a `shop_item`, "go skiing this winter" is a `wish_item`.
- **Promotion leaves the wish alive.** `+ Plan it` opens `UnifiedPlanModal` with `defaults={{ title }}`; `onSaved` always sets `status='planned'` and adds `promoted_task_id` **only when the save produced a `result.taskId`** (a schedule-only save with no linked task leaves the column null rather than storing a fake id), then invalidates **both** `['tasks']` and `['wish-items']`. The task is the commitment, the wish is the memory — the card then reads "→ scheduled". No trigger and no auto-close: a second completion signal beside `tasks.status` is the documented `user_tv_entries.current_season` mistake. `promoted_task_id` is `ON DELETE SET NULL` because `deleteTask` hard-deletes. A wish never passes through `useCreateTask`, so it never reaches Google Tasks.
- **"Passed" is grey, never red.** Page groups, in reading order: Open now · Coming up · Anytime · **Passed**. A wish whose `period_end` went by stays `'idea'` in the grey group under an explicit "nothing here is late" note; nothing auto-closes, nothing resets, and it leaves only when the user ticks or deletes it. `resolveWishWindow` (`wishRules.ts`) returns `open|upcoming|passed|anytime` from lexicographic `yyyy-MM-dd` compares — no `Date`, no timezone. `wishPeriodLabel` prefers `period_label` over the derived range.
- **Capture is the feature.** `WishQuickAdd` is always visible (never behind a "+ New" modal): title → optionally one season chip → Add, and **only the title clears** so a whole season's wishes go in one after another. Notes/place/priority/kind/status live in `WishSheet` (shared `Sheet`, keyed by row id so a background refetch can't overwrite typing) → `WishForm`. `WishFilters` (kind + status, default Active) only narrows what is **displayed** — "Everything" always brings the list back.
- **`shared/components/windowChips.tsx`** — the one-tap period picker, shared with the task-window control. `seasonWindows(today)` returns Northern-hemisphere/Oslo windows soonest-first (winter 1 Dec → end of Feb · spring 1 Mar–31 May · summer 1 Jun–31 Aug · autumn 1 Sep–30 Nov); `this <season>` = the season you are currently inside, otherwise the **upcoming** one, so tapping `❄️ This winter` in July stores 1 Dec this year → end of Feb next year (three anchor years are probed to cover a season that began last year). End-of-February is `endOfMonth`, **never a hardcoded `'02-28'`** — a leap year yields 29 Feb. `windowRangeLabel` is en-GB day-first ("1 Dec – 28 Feb"), adding the year to **both** ends only when a bound falls past next year (never a mixed "1 Dec 2029 – 28 Feb", which reads as a typo). `📅 Pick dates…` falls back to two `DateInput`s; `✕ No period` clears.
- **Resurfacing — two places, both purely additive.** (1) `DayView` renders one always-visible link when a period is open (`❄️ This winter · 3 things →`, from `useOpenWishes` — the label comes straight from `wishPeriodLabel`, emoji included; several open periods render `<first> +N more`). It belongs **there**: Daily's day band stacks `WeekStrip → DayAgenda → DayView`, so at 393px it is about one screenful down, while the glance board is 2–3 screenfuls down and `DayQuickRail` is `hidden xl:flex` — **neither is a phone surface, so don't "tidy" the row into them.** (2) `push-send`'s `buildMorning` appends a segment **only on the day `period_start` equals today** — once per season, never a daily nag; it adds a segment and adds no predicate to the tasks query. A missing table there surfaces as `{data:null,error}` (the null-check keeps the brief alive, **not** the `catch`). **`period_label` is stored WITH its season emoji** (`WindowChips` writes `❄️ This winter` verbatim), so anything that prepends its own emoji must strip the leading one first — `push-send`'s `labelText()` does, and a label-less row falls back to `A new period starts today (1 Dec – 28 Feb)` because a bare date range doesn't fit the `<name> starts today` sentence. The rest of that brief is still Turkish (`🌅 Günaydın`, `açık iş`) — sanctioned by the on-phone notification-text exception, so the body currently mixes languages until someone decides to translate the older segments.
- **Pre-migration safe.** `wishesApi.ts` returns `[]` on `42P01`/`PGRST205`/"Could not find the table" for **reads** (the `waterApi.ts` precedent), but a **write** throws a named "migration 069 not applied" error — a silent no-op would look exactly like data loss. `useWishes` and `useOpenWishes` share ONE query key `['wish-items']` and one `queryFn`, narrowed with `select`, so mounting both surfaces costs one request. Every mutation uses `useMutationWithFeedback`.
- **AI:** `wish_items` is `rw` in `ai-proxy`'s `DB_CATALOG` for the same reason `dev_requests` is — the user dictates wishes in chat, and the Siri `AI'a Sor` path runs `surface:'general'` with the **full** tool set, so one flat `db_insert` is the app's best capture channel — and it is in `EMBED_SOURCES` for `semantic_search` (needs one Developer → "Reindex AI search" click after the redeploy — embeddings are not written on insert). Its `rules` carry the "a period is a reminder window, never a deadline; never compute lateness, never auto-close on `period_end`" guardrail plus the **four-way** tie-break between the "remember this" targets: `dev_requests` (about this app) · `ai_memory` (a durable fact about the user) · `shop_items` (to buy) · `wish_items` (to do / a place to go). `ai_memory`, `shop_items` and `save_memory` gained the symmetric pointer back. Client-side, `aiApi.buildContext` adds a **`WISHES`** block (open windows then upcoming, each with its period/place/`(already planned)` flag, plus a count of undated ones). It selects only what a line needs, and a passed wish is left out of that **summary** while the block explicitly tells the model to `db_query wish_items` for the rest — a shorter summary, **not** a hidden row (`NEVER_HIDES` is about queries that decide what exists, not about a digest's length).
- **Verified:** `scripts/verify-wish-windows.cjs` (sucrase against the real modules, no test framework — the `verify-health-source-resolver.cjs` precedent) — **42 assertions**, all passing: season-chip math across a year boundary, leap-safe end of February (29 Feb 2028 vs 28 Feb 2027), both inclusive window boundary days, one-sided windows, and en-GB label formatting.

### Home Feature Detail

`HomePage.tsx` mounts, in a responsive multi-column layout: **DailyBriefing** (top), `TodaySummary` (the Daily glance board, reused), `WeatherWidget`, `RuterWidget` (transit), `CurrencyWidget`, `NewsWidget`, `RecentMediaWidget`, `GamesHomeWidget`, `TrainingHomeWidget`, `ProjectsHomeWidget`, `TodayScheduleWidget`. All widgets use `WidgetShell` + `useWidgetState` and **disable their queries when collapsed**.

- **Geolocation** (`useGeolocation.ts`): requests the browser's location once per session (React Query `['geolocation']`, `staleTime: Infinity`), falls back to Oslo if denied/unsupported. `HomePage` calls it unconditionally so the prompt fires on load regardless of which widgets are mounted. Weather uses it for a real local forecast; Transit uses it for "📍 Nearby" real stops (only when the fix is real, not the Oslo fallback).
- **Weather** (`WeatherWidget.tsx` + `weatherApi.ts`): current conditions + detail row (pressure, cloud cover) + 12h hourly strip + 5-day forecast (`computeDailyForecast`, MET Norway compact product, midday-representative symbol per day).
- **Currency** (`CurrencyWidget.tsx` + `currencyApi.ts`): **primary** NOK⇄TRY (direct — never routed through a third currency), **secondary** EUR⇄USD (their own parity), plus **gold** (XAU, confirmed available on the app's OXR plan via `/api/currencies.json`; shown as $/oz + NOK/gram with day-change badges). Replaced 5 confusing cross-rates that read as a wall of numbers in the briefing. `fetchCurrencyWeekTrend()` (feeds the AI daily briefing) follows the same shape.
- **Transit (Ruter widget)** — tabs Departures / Routes / **Via** / Settings. Current behaviour + the verified-live gotchas:
  - Saving a stop: `SettingsTab` has its own **"Add a Stop"** section and Departures shares the same `QuaySavePanel.tsx` (extracted so the quay/direction/label flow isn't duplicated), with one-tap **🏠 Home / 💼 Work** presets and small bordered cards in a responsive grid. Both tabs show a **"📍 Nearby"** row from `ruterApi.fetchNearestStops` (EnTur's `nearest` root query — **verified live via GraphQL introspection before use**, per the no-speculative-schema rule).
  - **Quay identity (verified live at Visperud):** roadside stops have `publicCode` `""` (empty string, **not null**) AND a null description on every quay, so any code/description-based key merges distinct directions into one bucket. `DeparturesTab` groups by the quay's **raw NSR id**; `fetchDepartures` normalizes `""` → undefined at the boundary. `fetchStopDirections` collects ALL distinct destinations per quay (not just the first seen) and a shared `quayLabel()` renders them consistently in `QuaySavePanel`, `DeparturesTab` and `RoutesTab`'s `PlaceDisplay`.
  - **Filter at the DEPARTURE level, not the group level** — scoping a saved favourite's `quay_id` at group level meant the merged single group either matched wholesale or fell through to show-all. There's a "Show all directions" toggle and an honest "saved direction not found — showing all" state instead of a silent fallback.
  - **Migration `049`** widened `user_transit_stops`' unique index to include `quay_id` (saving two directions of the same physical stop used to hit a raw Postgres duplicate-key error); `useTransitStops.addStop` throws a typed `DuplicateStopError` the UI catches to offer `updateStop()` instead. Same migration added `lat`/`lon` so **address favourites** are saveable, gated behind an "Include addresses" checkbox (`StopSearchInput`'s `stopsOnly` prop).
  - `useTransitRecentSearches` wires up the previously-orphaned `transit_recent_searches` table (last 6 stop→stop searches, chips in `RoutesTab`). Geolocation auto-fills `RoutesTab`'s "From" once (never fights a manual clear). Favourite Routes in `SettingsTab` are clickable (`onSelectRoute` → `RuterWidget` switches to Routes and applies it once via `pendingRouteId`). `TripCard`'s summary shows an explicit "Leave in X min" / "Leave at HH:MM" instead of one ambiguous bold time; live line disruptions (EnTur `situations` on `line`) render as severity-coloured badges in `TripCard` + `DeparturesTab`. **Travel Profile** (`useTravelProfile`, localStorage like `useDayTargets`) persists walking pace / max transfers / wheelchair-accessible and is applied to every `fetchTrips()` via `TripPreferences`. **Via tab** uses EnTur's verified `via: [TripViaLocationInput!]` argument (`gqlViaLocation()` builds `{visit: {stopLocationIds/coordinate}}`) and reuses `TripCard` unmodified — EnTur weaves the via stop into ONE coherent `TripPattern`.
  - **Real bug fixed:** `RoutesTab`'s "use current location" set an `'error'` state on GPS timeout/unavailable but rendered no message (only `'denied'` had visible text) — a silent failure violating the mandatory-toast rule; both paths now `toast.error(...)` on any geolocation failure.

### Media Feature Detail
Full-width layout (no max-width constraint). Key components:
- `MediaSearch` — movie/TV radio toggle, genre/year/rating filters, TMDB `/discover` or `/search`
- `CompactLibraryStrip` — 2-column status-group layout above Discovery (each column a flex-wrap poster strip, single column below `sm:`); collapse toggle. **Groups: Coming soon / Wishlist / Watching / Paused (TV) / Completed / Dropped.** **"Coming soon" is DATE-derived, not a manual status** (`isUnreleased` = release/first_air date in the future) — the fix for "Wishlist ≠ Coming soon": Wishlist = want-to-watch AND already out; Coming soon = not out yet. Deriving it also auto-resolves the legacy manual `upcoming` movie status (nothing ever migrated it, so a released film stayed pinned under a stale "Upcoming" label forever).
- `DiscoveryTabs` — Today/This Week/Popular/Upcoming/Norway tabs; manual sync only (24h stale)
- `EpisodesPanel` — season selector, per-episode watched checkbox (saves `watched_on`), plan checkbox → time blocks, Select All season. **Watched state is Netflix-style obvious**: season tabs show `n/total` + a green progress fill (✓ when complete); watched rows get a green tint, dimmed title and a filled green ✓ badge. Planning a **single** episode stamps `season_number`/`episode_number` on the created `time_blocks` row (migration `043`) — `source_id` alone identifies the show, not the episode, so this is what lets a DB trigger match "this episode got marked watched" back to exactly the one planned block and delete it. A multi-episode batch plan is intentionally NOT stamped and NOT auto-cleaned (deleting a 3-episode reminder because one was watched would be wrong); see AGENTS.md's "Linked-entity sync".
- `MediaDetailBody` — personal 1-10 rating, **private note** (`personal_note`, saves on blur), status buttons (movie completion stamps `watched_at`; TV completion/first-watch stamps `finished_at`/`started_at` — none of these columns were ever written before). Status pills: Wishlist/Watching/(Paused TV)/Completed/**Dropped**; the manual "Upcoming" pill was removed (coming-soon is date-derived). **"Advance to next episode"** uses `useNextEpisode` (real season rollover), fixing a bug where the last episode of a season recorded a nonexistent `S?E(n+1)` instead of `S(n+1)E1`. Star fill is `accent-500`.
- `TonightPicker` (random from My List / Trending / Popular) · `ReleaseCalendar` (upcoming from wishlist) · `MediaStats` · `MediaBackdrop` (rotating backdrops, fixed, low-opacity crossfade)

DB tables: `movies`, `user_movie_entries` (watching/wishlist/completed/dropped/upcoming — `upcoming` is movie-only by design), `tv_series`, `user_tv_entries` (watching/wishlist/completed/dropped/paused), `user_tv_episodes` (season_number, episode_number, watched_at — **the ONE source of truth for TV progress**). Both entry types have `rating int (1-10)`, `genres jsonb`, `personal_note`, `priority`.

**TV progress architecture (migration `050`, root-cause fix)**: THREE disjoint progress systems used to coexist — a legacy `watched_episodes` table (bulk-marked data incl. The Office's 169 rows lived ONLY there; no current code read or wrote it), `user_tv_episodes` (what the UI writes), and `user_tv_entries.current_season/current_episode` cache columns that migration 011 promised "application logic" would maintain but never did — so Daily Watch-next, the AI briefing, Ask-AI context, `get_media`, the detail chip and MediaStats all showed S1·E0 forever despite hundreds of real watched rows. Migration 050 backfills `user_tv_episodes` from the legacy table (union — each had rows the other lacked), DROPS the legacy table, installs a trigger keeping the cache synced to max-watched, one-time recomputes it, and adds the missing UPDATE RLS on `movies`/`tv_series` (metadata upserts silently couldn't refresh — rows were frozen at first insert). `markEpisodeWatched`/`unmarkEpisodeWatched` also sync the cache app-side (instant UI, works pre-migration). Anything needing "next unwatched" uses `useNextEpisode` (max watched + TMDB season data → SxxEyy/title/air-date, rolls into the next season, `caughtUp` when none). **The cache columns are for cheap reads only — never write them directly.**

TMDB images: `posterUrl(path, size)` from `src/integrations/tmdb/client.ts`.

### Shop Feature Detail
Wishlist/shopping-planner under `src/features/shop/`. Strict **2-level** category tree — `shop_categories.parent_id` null = top category, set = subcategory; items always attach to a subcategory (`shop_items.category_id`), never to a top category. `ShopPage` is a fixed two-pane layout (`h-full`, sized by the layout's flex slot, not a viewport calc): left = `ShopAIBox` chat panel (fixed width on desktop, fixed height on mobile, internal scroll + pinned input — never grows the page), right = category pills + wishlist grid (own scroll).
- `ShopAIBox` — chat panel (separate from the app-wide ✦ Ask AI panel) using `sendShopMessage` with a shopping-companion system prompt: converses about plans (not just add-item commands), handles a pasted multi-item basket, never guesses a new category silently.
- Clarifying questions use a real Gemini function call — `ask_clarifying_question(question, options[])` — not text-parsed conventions. `ai-proxy`'s loop **short-circuits** on this call (the answer must come from the human as a real next turn, not a synthesized tool response) and returns `{ text, quickReplies }`; `ShopAIBox` renders `quickReplies` as tappable buttons.
- `AddShopItemModal` — manual add: top category / subcategory cascading selects, each with "+ New…" to create on the fly. `ShopItemCard` — title, notes, price (+ `price_source` `manual`|`ai_estimate`), platform, link, priority dot, region flag (🇹🇷/🇳🇴), planned date, mark bought/delete.
- No live price-lookup API (Prisjakt/Akakçe have no public free API) — price is manual entry; the AI can only give a rough text estimate on request, never auto-writes a price.
- DB: `shop_categories` (id, user_id, name, parent_id), `shop_items` (category_id, title, notes, price, price_source, platform, url, priority, region `TR`/`NO`, planned_date, status `wishlist`/`bought`/`dropped`, source_type `manual`/`ai`). Migrations `029_shop.sql` + `030_shop_seed_categories.sql` (partial-unique indexes + a starter taxonomy, seeded idempotently for every existing user).
- AI tools (shared with the main Ask AI panel): `get_shop_categories`, `create_shop_category`, `create_shop_item`, `ask_clarifying_question`.

### Food Feature Detail

User-visible name is **Food** (nav tab, CommandBar, page h1); route stays `/recipes` for deep links. `RecipesPage` has 4 tabs: **Today** (default) · **Library** · **Ingredients** · **Meal Plan**, plus `FoodTabs` (Food | Shop) in the header row.

**Data model — 4 tables** (after migration `061` unified plan + diary):
- `recipes` — title, description, `servings` (base count), instructions, `macro_mode` `manual`|`from_ingredients`, per-serving `calories`/`protein_g`/`carbs_g`/`fat_g`/`sugar_g`/`fiber_g`, `category` (breakfast/lunch/dinner/snack/supplement), `image_url`, `source_url`, `times_cooked`, `is_temp`.
- `recipe_ingredients` — name, `quantity` (nullable = to-taste), unit, note, sort_order, `library_ingredient_id` (nullable FK). Quantities are stored for the base `servings`; recipe macros are stored **per serving**.
- `recipe_ingredient_library` — reusable ingredient catalog, macros **always per 100g**, unique per `(user_id, name)`, + `fiber_g`, portion presets (`serving_label`/`serving_grams`, e.g. "1 scoop"=30g), `food_group`, `image_url`, and `source`/`source_ref` provenance (`off`, `dsld`, `matvaretabellen`, user's own; user's own wins on name conflict, attribution travels on the row, idempotent refresh).
- **`food_log_entries` — plan AND diary in one table**, discriminated by `status`: `'planned'` (intent; macros computed live on read) vs `'eaten'` (a real at-log-time **snapshot**). Planning → eating is a status flip. Slots: breakfast/lunch/dinner/snack/**supplement**; unlimited rows per slot; exactly one of `recipe_id` / `library_ingredient_id`+qty+unit / `custom_title`.
- **`water_log_entries`** (migration `067`) — one row per logged amount; the day's total = `SUM(amount_ml)`. **Deliberately its own table, never a food slot** — water carries no macros and must never enter the calorie ring. Owner-only RLS, `user_id DEFAULT auth.uid()`, no audit trigger (high-frequency, low audit value — same reasoning as the bulk-sync exemption).

**Load-bearing contracts (do not break):**
- **Macros are snapshotted at log time.** Editing a library ingredient or a recipe never rewrites history.
- The original `UNIQUE (user_id, date, meal_slot)` on the old plan table **had to die** — it physically forbade "lunch = chicken + rice + broccoli".
- **Planned rows render muted "planned" and are NEVER added to the eaten total** (the ring sums `eaten` only) — counting them was visually contradictory with the ring.
- `dayNutritionApi`'s plan-side query deliberately does **NOT** select `recipes.fiber_g` (a missing column would error the whole plan query on old schema; plan rows don't feed the ring anyway).
- **Pre-migration fallback pattern (reused across the feature):** on a column-missing error (`42703` / `PGRST204`) retry once with the new column dropped — `createRecipe`/`updateRecipe` for `fiber_g` and `is_temp`, `create/updateIngredientLibraryItem` for `image_url`, and `foodLogApi`/`mealPlanApi`/`dayNutritionApi` for `status` (they fall back to the old two-table behaviour). This is what let the frontend deploy before migration 061 was applied.
- **Cache-key bug (real):** food-log hooks invalidated `['day-nutrition']`, which does NOT prefix-match the card's `['meal-plan','day-nutrition',date]` key — the card showed stale totals. Invalidate the actual key.
- `macro_mode: 'from_ingredients'` is computed on save (`computeMacrosFromIngredients` in `recipesApi.ts`): sum each linked library ingredient's per-100g macros × `quantity/100`, ÷ servings. Only rows with a `library_ingredient_id` AND a weight/volume unit contribute; the rest are skipped and surfaced in the UI. `RecipeModal` previews the same math client-side (`previewMacros`). `dayNutritionApi` imports recipesApi's `WEIGHT_UNITS` (a narrower local copy once dropped 'milliliter(s)').

**Components / flows:**
- **`FoodLogModal`** — the build-a-meal logger (`sm:max-w-xl lg:max-w-3xl`, 2-column on `lg`: left = recents / saved meals / search / new ingredient, right = the basket). **Recents-only suggestions** — the alphabetical library dump was removed; ingredient match chips render only while searching. Grams or portion-preset tap; live kcal/protein total; footer shows remaining protein/kcal **after** this meal. "Save meal" is **save-only** (no auto-log) with a "Makes N portions" field, so a batch is stored at correct per-serving macros and you log your portion later.
- **Temp meals (migration `066`, `recipes.is_temp`)** — a saved meal is **temp by default**: hidden from the Library grid (`RecipesPage` filters `!is_temp`) but fully usable — it appears in the logger's "Saved meals" strip, and hover (desktop) / the ✎ editor (mobile, `onEditRecipe` → RecipeModal) reveal its ingredients. An inline "Library" checkbox on the save row promotes it to a permanent Library recipe.
- **`MealPortionPicker`** — tapping a saved meal picks a free **%** of the WHOLE batch (25/50/75/100 chips + free entry, + grams when total weight is computable) → `logRecipe(recipe, servingsEaten)` where `servingsEaten = pct/100 × recipe.servings`. `RecipeDetail`'s "I ate this" is a free-entry portions input with a "% of the batch · logs N kcal" hint.
- **`EditFoodLogModal`** — edit a logged row (was delete-only): kind inferred from the row (library → edit grams + re-snapshot · recipe → edit servings + re-snapshot · custom → edit title+macros), and any of them can move meal slot. Wired to ✎ on every diary row (Daily NutritionCard, FoodTodayTab, MealPlanWeek's eaten chips). `DayMeal` carries a `logEntry` sub-object (raw ids/qty/unit) so the modal knows the row kind.
- **`MealPlanWeek`** — 7-day × slot grid; each cell shows 📋 planned (tap → `AssignMealModal`) + green ✓ eaten chips (tap → edit), because a Today-logged day used to leave the plan grid empty. Empty cell = the whole area is one big centered `+`; filled cell gets a full-width bottom "＋ add" row. Every planned row has a **✓ confirm** (`mealPlanApi.eatPlannedEntry` / `useEatPlannedEntry`) that computes a fresh snapshot and flips planned→eaten so it starts counting in the ring. `AssignMealModal` modes: 🍲 Recipe / 🥚 Ingredient / **✏️ Type it** (free-text one-off).
- **`FoodTodayTab`** — 2-column on `xl` (summary + coach left, meal slots right): small calorie + protein **rings**, macro chips with fiber inline after fat (green dot), sugar, `WaterTracker`, an inline **⚙ Goals** editor (goal pills + calorie/protein steppers) that explains how the numbers are derived, and the coach card. Its own `DateNav` drives `foodDate`.
- **`useNutritionCoach` (bodyweight-driven coaching)** — reads the latest real `weight_body_mass`; the **goal** (Maintain/Cut/Gain, in `useDayTargets` localStorage) drives a protein suggestion (1.8 g/kg maintain/gain, **2.4 on a cut** — panel-set; g/kg of BODYWEIGHT, so it over-prescribes at high body fat) and an **adaptive-calorie nudge** from a 28-day least-squares weight trend (kg/wk). The nudge is gated on EVERY guardrail: intake logging (≥4/7 days) AND weight-signal quality (≥10 weigh-ins spanning ≥14d) AND a 14-day cooldown (`lastCalorieAdjust`), never crosses the calorie floor (`max(1500, 24×kg)`), and shows an honest on-track / at-floor / gate state instead of a bare number. Fat floor (~0.6 g/kg) on a cut; per-meal protein hint (~0.4 g/kg). All numbers are practitioner heuristics except the protein figures + the Garthe loss-rate ceiling (trial-backed). The coach card **always renders**: no bodyweight → "Add a bodyweight in Training → Body" CTA; gates unmet → the specific reason; else the applyable suggestions. Targets live in localStorage, so the AI can recommend but cannot set them — it must tell the user to tap Goals → Apply.
- **Ingredient sources** (all reviewed before save, per-100g): the user's **library** (search-first) · **barcode → Open Food Facts** (`openFoodFactsApi.lookupBarcode`, free/no-key/CORS-open → no edge function) via `BarcodeScanner` (native `BarcodeDetector` + dynamically-imported `@zxing/browser` fallback for iOS, code-split; manual entry always available) · **online name search** (`openFoodFactsApi.searchFoodsByName`, OFF v2, retries on 503 with an honest "busy" state) merged behind **Kassalapp** results (Norwegian branded groceries + price + nutrition) via the `food-search` edge function; `OnlineFoodSearch` is wired into FoodLogModal's new-ingredient (🔎) and the Ingredients manager (📷 + 🔎) · **NIH DSLD** for supplements (`supplementDsldApi.ts`, `api.ods.od.nih.gov/dsld/v9` — free, no key, CORS-open, **browser-direct, no edge function**; live-verified per the no-speculative-schema rule: `search-filter?q=` → 3957 creatine labels, then `label/{id}` → per-serving macros + serving grams + active-ingredient grams; US labels only). Gram-based products (powders) become reusable per-100g library rows (`source='dsld'`, `food_group='Supplements'`, deduped by `source_ref`) logged by grams; count-based capsules log as a reusable custom diary line. **Kassalapp caveat:** the response STRUCTURE is verified from the `kassalappy` client's Pydantic models (`Product{name,brand,image,ean,current_price,nutrition:NutritionItem[]}`, `NutritionItem{code,display_name,amount,unit}`) and `normalize()` disambiguates kcal vs kJ by unit — but the exact Norwegian `display_name`/`code` VALUES (assumed Energi/Protein/Karbohydrater/Fett/Sukker/Kostfiber) and the per-100g basis are **still unconfirmed against a live sample**; unknown labels fall back to null, never crash.
- **Supplements** log into the `supplement` slot through the normal logger (and `phone-gateway`'s `log_supplement`). The standalone 💊 launcher button was removed from both the Food header and the Daily NutritionCard. **Dead code:** `src/features/recipes/components/SupplementModal.tsx` still exists but is mounted nowhere — delete it or re-wire it; don't assume it runs.
- **Recipes core:** `RecipeModal` (create/edit — title, base servings, dynamic ingredient rows, inline `NewIngredientInline` in from-ingredients mode, full macro labels Calories/Protein/Carbs/Fat/Sugar/Fiber — never P/C/F abbreviations; save replaces all ingredient rows) · `RecipeDetail` (serving stepper rescales quantities × target/base and totals macros per-serving × target) · `RecipeCard`/Library grid (`repeat(auto-fill,minmax(10rem,12rem))`, client-side search over title/description/ingredient name, category filter pills) · `RecipeBackdrop` (rotating header photo built from the user's OWN recipe `image_url`s, gradient+emoji fallback — no hotlinked stock photo) · `MacroBar` (proportional protein/carbs/fat stacked bar, 4/4/9 kcal-per-gram) · `CookMode` (full-screen step-by-step: large text, progress dots, persistent ingredient checklist) · `times_cooked` + a 🔥 "I made this" button (organic favourites signal, no extra table) · **pantry → Shop** (`recipes/api/shopIntegration.ts`, deliberately crossing into `shop/api/shopApi.ts`): a per-ingredient "I already have this" checkbox (session state) and "Add missing to Shop" pushes unchecked ingredients into a `Groceries → Recipe Ingredients` category with quantity + recipe title in the notes.
- **AI recipe extraction (editor-only)** — `parseRecipeText(text)`, `parseRecipeFromUrl(url)` (fetchUrl → parse), `estimateRecipeMacros(ingredients, servings)` in `aiApi.ts`, on top of `ai-proxy`'s `responseSchema` + `fetchUrl` modes. The parse prompt always forces Turkish output (title/ingredients/instructions translated regardless of source language). In `RecipeModal`: "✨ Paste recipe" (Paste text / From URL toggle; URL mode also sets `source_url`) and "✨ Estimate with AI" (manual macro mode only). All are one-shot fills the user reviews — **never call these from the general Ask AI chat panel**, they're editor-only actions.
- **General Ask AI recipe tools** are separate: `get_recipes`/`create_recipe` in `ai-proxy`, for when the user pastes/describes a recipe in normal chat. The system prompt carries an explicit guardrail: **a shared recipe is never `create_shop_item`/`create_shop_category`** — this was a real bug (a pasted recipe got mis-filed as a Shop wishlist item under an AI-invented "Home & Living > Recipes" category, since no recipe tool existed for that panel yet).
- **Seed data:** migration `035_recipe_seed_turkish_classics.sql` seeds 10 classic Turkish dishes with ingredients + editorial per-serving macro estimates (`macro_mode='manual'`) for every existing user, idempotent (no Turkish recipe site has a public API and neither Spoonacular nor Edamam tags "Turkish" as a cuisine). Migration `054` seeds ~50 Norwegian staples — the full 2121-food **Matvaretabellen** import (`scripts/generate-matvaretabellen-seed.mjs`, re-run after the annual autumn update; sugar mapped to Mono+Di total, carbs to Karbo available) was **never applied to prod**; prod holds the ~50 staples + the user's own foods. **There is no migration `056`.** The AI prompt says "search the library first" without over-claiming its size.
- **Table naming standard:** every satellite table is `recipe_*` (matches `shop_*`/`hevy_*`); `recipes` itself is the feature's root entity (like `tasks`/`movies`/`projects`). Built fresh in Supabase — RecipeSage was evaluated and rejected (AGPL + a separate tRPC service needing its own server); no consumer recipe app has a usable public API.
- **Known low-value:** `food_log_entries.notes` is effectively write-only. Water is **not** in `ai-proxy`'s `DB_CATALOG`, so the AI cannot currently read or write water logs (the phone gateway can).

### Training Feature Detail

Page layout (`TrainingPage`): faint training-photo header banner; **Hevy / Strava / Health / 🧠 Coach** pill tabs + Sync/Settings on the **left**; content column (`max-w-4xl`, left-aligned) with `TrainingCalendar` **pinned to the right edge (440px)**, independent of the active tab. `HevyTab` sub-tabs: Workouts, Routines, Personal Records, Muscles, Body, Exercises.

**WIDTH STANDARD PILOT (decision pending — DO NOT spread beyond Training without explicit user approval; see the "Alan/yoğunluk standardı" dev request).** What stands here: Workouts = content-width card columns (`repeat(auto-fill,minmax(19rem,22rem))`, leftover space stays right); Personal Records = the same fixed-width column flow (15–18rem) + a hover/tap peek card (GIF + est-1RM) + capped search/banner; Muscles = container queries (`@container` root, `@3xl:` row, `@4xl:` 2-col overview); Body = bento auto-fill grid + a container-query-adaptive chart + a View-Transition zoom that keeps the chart's aspect ratio and caps at `max-w-4xl` (a full-width stretch was rejected). `NextSessionBanner` sits in the right rail under the calendar, not as a full-width band. See Coding Rules → Width Standard for the 6 rules themselves.

- **AI PT Coach** (`PTCoachTab.tsx` + `api/ptCoachApi.ts`): user-initiated (NEVER auto-runs) daily assessment acting as a decisive personal trainer. **Token-minimal BY CONSTRUCTION** — `buildTrainingSnapshot()` pre-aggregates everything client-side (latest workout's per-exercise set summaries with previous-same-exercise comparison from up to 10 sessions back, weekly hard-sets-per-muscle vs `MUSCLE_LANDMARKS`, last-night + 7d-avg sleep, steps/active energy, 30d weight trend) into ONE compact text block → a single one-shot `invokeAI` call, zero tool loop (same pattern as the daily briefing). The coaching brain (recovery gate on <6h sleep, MRV deload cues, double-progression +2.5/+5kg engine, volume steering vs MEV) was distilled by the `strength-coach` agent and bounded by the `sports-scientist` agent's evidence guardrails — **the snapshot line formats are a CONTRACT with that prompt; change them together.** Input: feeling chips + optional free text. Every assessment is saved to `pt_assessments` (migration `051`: date, feeling, note, the exact snapshot the coach saw, the reply, which model served it); the next run feeds the previous one back in and the prompt's FOLLOW-UP rule makes the coach open with whether its last advice was applied. History is browsable in the tab; a localStorage fallback still displays results if the migration isn't applied.
- **Coach mode in the main chat** (`AIPanel`'s 🏋️ toggle → `sendCoachMessage`/`buildCoachContext`): a SEPARATE surface from the daily assessment — open conversation over a prepared last-30-days compact JSON (workouts w/ sets, current routines incl. ids, sleep, steps, active kcal, weight/body-fat, the diary's logged nutrition, the coach's own past assessments, the athlete profile + active limitations below; ~7k chars measured live), blunt science-over-feelings persona, and REAL program edits via `update_hevy_routine` plus new-routine creation via `create_hevy_routine`. Both forward to `hevy-api` so every documented Hevy payload trap stays handled in one place and **Hevy stays the source of truth** (the local mirror is upserted by hevy-api, never written directly); both confirm first, and create requires resolving real `exercise_template_id`s via `db_query` on `hevy_exercise_templates`. The general Ask AI chat also carries a TRAINING QUESTIONS persona block (client-side in `aiApi.ts`'s `SYSTEM_PROMPT`, no edge deploy needed), which now tells the model to check the `get_athlete_profile` tool before giving programming advice.
- **Athlete profile** (migration `070`: `athlete_profile` + `athlete_limitations`) — a second strength-coach/sports-scientist design pairing, this time over durable facts about the person rather than volume math. **Two tables, not one `text[]` column**, because the repo's real convention for a one-to-many attribute list is a child table (`recipe_ingredients`, `hevy_exercise_template_muscles`), and a limitation needs its own identity/`created_at`/CHECK that an array element can't carry. `athlete_profile` is a true **singleton** — `user_id` is the PRIMARY KEY (no `id` column) — holding `goal` (strength/hypertrophy/fat_loss/general, read-only metadata for the coach's tone — it never forks the volume algorithm), `experience_level` (novice/intermediate/advanced), `training_age_years`, `training_days_per_week`, `equipment_access`, `notes`. `athlete_limitations` is a normal list: `movement_pattern` + `severity` (`avoid`/`limit`/`monitor`, **defaults to `monitor`** — escalating to avoid/limit is always an explicit, separate update, never automatic on log) + `note` + `active`.
  - **`movement_pattern`, never a muscle slug.** A physical limitation is almost always movement-shaped, not muscle-shaped — a shoulder issue rules out overhead pressing, but flat press/flyes are frequently still safe despite training the same chest muscle; flagging at the muscle level would suppress a still-safe stimulus. `muscleMap.ts`'s `MovementPattern` (9 values: squat/hinge/horizontal_press/vertical_press/horizontal_pull/vertical_pull/lunge/carry/isolation — a standard S&C taxonomy, not exotic) is free text on the DB side (no CHECK enum — a fixed value list would force an `ai-proxy` redeploy for every new phrasing a real conversation surfaces, the same reasoning `069`'s rejected season-CHECK already established).
  - **`PATTERN_AFFECTED_SLUGS`** (`muscleMap.ts`) is the derivation seam: which `Slug`s a pattern restriction realistically touches, and how hard (`'avoid'` = no comparable substitute at heavy load; `'limit'` = other patterns/isolation work still train it safely). This is what lets `WorkedMuscles.tsx` — a pipeline keyed entirely by muscle `Slug`, with zero pattern dimension of its own — render a per-muscle flag without the DB ever storing a muscle slug directly. A flagged muscle is a **third visual state** (violet, `FLAG_META`), deliberately never green or red: most flagged muscles are still safely trainable through a non-conflicting exercise, so a danger colour would overstate the restriction. Shown even at 0 logged sets (the flag is the point, not the volume).
  - **Experience-scaled landmarks** (`scaleLandmarksForExperience`, `EXPERIENCE_MULTIPLIER`): novice ×0.85 / intermediate ×1 / advanced ×1.15, applied to **MEV and MRV only** — MV and MAV are deliberately left unscaled, a cross-examined agreement (RP framework: training age shifts MEV/MRV; scaling the WHOLE landmark set would overclaim precision on a table that's already a heuristic, not a measurement). Every `InfoBubble` showing an adjusted landmark says so explicitly: *"Adjusted ±15% for your experience level — an unvalidated adjustment on top of an already-heuristic baseline, not a measured number."*
  - **`ptCoachApi.ts`'s snapshot** gets one new block — a `PROFİL:` line (goal/experience/equipment/days, only the fields actually set) + one `Kısıtlama:` line per active limitation — inserted before the workout lines, respecting the file's snapshot-line-format contract; `PT_SYSTEM_PROMPT`'s rule 1 (safety) was extended to ground `avoid`/`limit`/`monitor` into concrete behaviour. Absent entirely when the user has never touched the profile — zero snapshot-line change for anyone who doesn't use this feature.
  - **`ai-proxy`**: `athlete_profile`/`athlete_limitations` are `rw` `DB_CATALOG` entries (per the user's explicit instruction to give the AI authority here) plus a new single-shot tool, `get_athlete_profile` — returns both tables in ONE call (mirroring `get_day_summary`'s shape) instead of two separate `db_query` round trips, because token efficiency was an explicit requirement. `athlete_profile` being a singleton keyed by `user_id` (no `id` column) means `db_update` needs a non-empty `filters` object even though the function already scopes to `user_id` internally — the catalog's `rules` text spells out the exact `filters={"user_id":...}` shape to use. Not added to `EMBED_SOURCES` (semantic search) — this is structured settings data consulted deterministically, not free text worth fuzzy-recalling.
  - **Settings UI**: `PTCoachTab.tsx` gained a slim summary strip (`🎯 hypertrophy · intermediate · gym · 2 limitations` + a ⚙ button) opening `AthleteProfileSheet.tsx` (pills/stepper autosave on tap, `notes` autosaves on blur — matching Media's `personal_note` convention, no Save/Cancel footer) and `LimitationsList.tsx` (every limitation stays visible even when inactive — muted, one tap to reactivate; deleting is a separate confirmed action, never implied by "inactive"). Pre-migration-safe throughout: every new read degrades to `null`/`[]` on a missing table, every write throws a named "migration 070 not applied" error rather than failing silently.
- **Muscles sub-tab** (`WorkedMuscles.tsx` + `muscleMap.ts`): a stylised front/back body (`react-muscle-highlighter`, MIT) on a dark panel, coloured by an **evidence-based training-VOLUME algorithm** (designed with the `strength-coach` + `sports-scientist` agents, `.claude/agents/`). **Not** a self-relative heat ramp — an **absolute diverging scale** vs per-muscle landmarks. Currency = **weekly hard working sets** per muscle (warm-ups excluded via `hevy_sets.type`); each exercise credits its PRIMARY muscle 1.0 and each SECONDARY 0.5 via the pluggable `contribution(templateId, slug, role)` seam (`ROLE_WEIGHTS`; future: per-exercise-per-muscle % from `CONTRIBUTION_OVERRIDES`/DB, incl. tertiary). Window total ÷ (days/7) → weekly-equivalent, compared to `MUSCLE_LANDMARKS` (per-muscle MV/MEV/MAV/MRV, RP framework) → 6 diverging **bands** (`BANDS_META`: grey not-trained · blue below-maintenance · teal maintenance · **green optimal MEV–MAV** · amber high · red over-MRV). Period 30 days (default) / 90 days, data via `hevyApi.fetchMuscleVolume(from,to)` (paginated). UI (details never skipped, per user): diverging legend + ℹ️ `InfoBubble` popovers explaining colours/caveats, under-dosed & over-MRV lists, push:pull + quad:ham balance chips, side-filtered top chips dotted with band colour, per-muscle detail (sets/wk + band + target MEV–MAV + exercises with set counts and `ExerciseThumb` GIFs + training-day history), and an "unattributed work" line for cardio/full_body/other (primary dropped, secondaries still count). `HEVY_TO_SLUG` covers the COMPLETE Hevy `primary_muscle_group` enum (20 values, verified against OpenAPI + live DB: 0 unmapped, 0 missing templates).
- **Exercise demo GIFs** (`exerciseMedia.tsx` → `ExerciseThumb`): Hevy's API returns no exercise media, so animated GIFs come from **JahelCuadrado/ExerciseGymGifsDB** (1323 exercises, jsDelivr CDN, CORS-open, free — no key/proxy). `useExerciseImageDb` loads `api/en/exercises.json` once; `matchExercise` fuzzy-matches Hevy titles by normalized name tokens + an equipment bonus (threshold 0.5) with a graceful no-match fallback. Tapping a thumbnail opens a modal with the larger GIF + instructions. Wired into `ExerciseTemplatesTab`, `RoutinesTab`, `HevyWorkoutDetail`, `HevyPRList`, `HevyTab` and the Muscles detail list. **Every GIF URL is anchored to the single `GIF_SOURCE` constant** (`https://cdn.jsdelivr.net/gh/JahelCuadrado/ExerciseGymGifsDB@v1.1.0`) — self-hosting later = mirror the repo into Supabase Storage and swap that one constant, no UI/matching changes.
- `TrainingCalendar` — week/month; Hevy workouts + Strava activities + **planned sessions** (`time_blocks` where `category='training'`) as time-coloured dots (upcoming=blue, today=green, past=red). Click a workout → `HevyWorkoutDetail`.
- `RoutinesTab` expanded view: one bordered row per exercise — order number + demo GIF + set chips + notes. The whole Hevy tab fills width up to the calendar rail on 2xl+ (`TrainingPage`'s `wide` flag).
- **`RoutineModals` — Hevy payload traps (each a real 400):** the payload must match the Hevy OpenAPI schema exactly (no extra keys). Set fields = `type, weight_kg, reps, rep_range:{start,end}, distance_meters, duration_seconds, custom_metric` (NO `rpe`/`index`); exercise = `exercise_template_id, superset_id, rest_seconds, notes, sets` (NO `index`/`title`). (1) **`rep_range: null` → 400** "must be of type object" — send `rep_range` only when a range is entered, otherwise OMIT the key and send fixed `reps`. (2) **`folder_id` is not allowed on PUT** (400 "not allowed") — valid on create (POST) only. `formToPayload` builds payloads this way; `hevy-api` also defensively strips null `rep_range` and drops `folder_id` from update bodies. Set inputs render per exercise `type` via `setFieldsForType`.
- Hevy exercise `type` is the `CustomExerciseType` enum (`weight_reps`, `reps_only`, `bodyweight_reps`, `bodyweight_weighted`, `bodyweight_assisted`, `duration`, `weight_duration`, `distance_duration`, `short_distance_weight`, `floors_duration`, `steps_duration`) — typed as `string`, humanised in `ExerciseTemplatesTab`.
- Edge functions normalise Hevy responses (`unwrapEntity`) — Hevy may return an entity directly, wrapped, or array-wrapped. Incremental sync reads `event.workout` (updates) / `event.id` (deletes).
- **Body measurement payloads (three stacked real bugs, live-verified):** (1) Hevy 400s on ANY `null` field in `PUT /v1/body_measurements/{date}` ("Expected number, received null") — the form used to send the full field set with nulls for blanks, so EVERY save failed regardless of input; empty fields must be **OMITTED** (partial entry is legit — both `handleSave` and `hevy-api` strip them). (2) That PUT returns **2xx with an EMPTY body** — `hevyRequest`'s unconditional `res.json()` threw "Unexpected end of JSON input" AFTER a successful write, so even a valid full form reported an error; `hevyRequest` now parses tolerantly (empty/non-JSON → null). (3) **MERGE semantics** — Hevy's PUT replaces the WHOLE day and the DB upsert used to null out omitted columns, so a partial save (e.g. tapping the weight suggestion chip alone) silently WIPED the other values already recorded for that date in both Hevy and the local DB (**real data-loss incident**). `hevy-api` now loads the existing row for the date, overlays only the provided fields, and sends/stores the full merged set; the modal prefills stored values for the selected date so you see what a save keeps. Suggestion chips only fill a form field — they never write anything. Form fields are `type="text" inputMode="decimal"` with a digits+single-dot sanitizer (comma→dot) plus one-tap chips prefilling latest weight/body-fat from Apple Health (`weight_body_mass`/`body_fat_percentage`, 60d window).
- **Sync scope:** workouts use the events-delta feed (`/v1/workouts/events`). Routines, routine folders and body measurements have NO event feed, so `hevy-incremental-sync` also re-fetches those three collections in full and upserts them on every Sync (a routine created in the Hevy app appears on Sync, no full re-import needed). Exercise templates still only come via full re-import.
- **Planned session ↔ task auto-link:** `RoutinesTab`'s "Plan routine" creates a task with `source_type='training_session'`, `source_id=routine.id` (migration `039`). When the matching Hevy workout is logged from that routine, the inlined `upsertWorkoutToDb` deletes the task + its linked `time_blocks` row automatically (see Edge Functions). Freeform workouts (no `routine_id`, or a mismatch) can't be auto-matched — `HevyTab`'s Workouts sub-tab shows a manual close-out button when an open training-session task falls on the same calendar day. **Separately** (migration `043`, full design in AGENTS.md's "Linked-entity sync"): deleting the plan's `time_blocks` row directly also cascade-deletes its auto-created task, and editing the plan's date/time keeps the task's `due_date`/`due_time` in sync both ways — a DB-trigger guarantee, so it holds regardless of which door the delete/edit came through.

DB (Hevy): `hevy_workouts`/`_exercises`/`hevy_sets`, `hevy_routines`/`_exercises`/`hevy_routine_sets` (routine sets also have `rep_range_start/end`), `hevy_exercise_templates`(+`_muscles`), `hevy_routine_folders`, `hevy_body_measurements`, `hevy_workout_events_cursor`.

#### Health data (Apple Health + Fitbit Air)

**Two ingest paths, one storage model.** Huawei's own Health Kit API is closed to individual developers (requires an AppGallery-published app), so: Huawei Health on iOS syncs into Apple HealthKit → the **Health Auto Export** app reads HealthKit and POSTs to `health-export-webhook`. Separately the **Fitbit Air** writes to Google's cloud, polled by `google-health-sync`. Both land in `health_workouts` / `health_metrics` / `health_sleep_segments` (shared `HEVY_USER_ID`, separate from Hevy's tables).

- **Point-in-time grain, not daily** (migration `041`): `health_metrics` was originally one row per `(metric, day, source)` — a real bug confirmed against production exports; every new point for the same day silently overwrote the previous one, so only the LAST point of the day survived (`active_energy` for a full day showing 0.05 kcal instead of ~230). Fixed by storing every incoming point as its own row keyed `(user_id, metric_name, recorded_at, source)`. `date` stays a plain column **computed from the export's own local-time string at ingest, not derived from the UTC `recorded_at`** (avoids a midnight timezone-shift bug). No aggregation at ingest.
- **`source_family` (migration `062`) — values are `apple | fitbit | manual`** (CHECK-constrained). Never `apple|google`; the pre-implementation design doc's naming was different. The column is `NOT NULL DEFAULT 'apple'` **and** explicitly stamped by `health-export-webhook` (belt-and-suspenders, not either/or). Existing rows are legitimately all `apple` — Huawei data already flows through Apple HealthKit before reaching us. Migration 062 also added `health_sleep_segments` (per-stage rows, `source_record_id` from `metadata.externalId` + a natural unique key for idempotent re-delivery; no audit trigger — bulk-sync exemption).
- **Aggregation happens at query time**, per metric, via `healthMetrics.ts`'s `METRIC_AGGREGATION`: `sum` (cumulative — steps/energy/distance/durations), `average` (rates/levels — speeds/dB/physical effort/respiratory rate/HRV), `minmaxavg` (`heart_rate`'s Min/Avg/Max points, and SpO2 — a locked guess pending live shape confirmation), `latest` (point-in-time — weight/body fat/BMI/resting HR), `sleep` (`sleep_analysis`'s own stage-duration merge). **Unrecognized metrics default to `latest`, the safest fallback.** The pure functions live in `healthAggregate.ts` (`computeDailySeries`/`computeHourlyBuckets`/`computeHeartRateDailySeries`/`computeSleepSummary`) and deliberately ignore `source` when merging (a day can have differing source strings, e.g. `"Furkan's Apple Watch"` at midnight vs `"Furkan's Apple Watch|Lasci"` once a second device joins — the UI shows one number; raw rows stay source-separated in the DB).
- **Source resolution — `resolveSourcePoints` + `healthSourceDefaults.ts` (cardinal rules):**
  - Resolution is **stream-level** (one raw `source` string, not a whole family — live data showed two same-family streams, even two Watch-labelled ones, writing the same hours) and per-**window**: strategy `'bucket'` (hourly) for cumulative/continuous metrics, `'day'` for sparse/point-in-time, `'night'` for sleep. Exactly ONE stream wins each window; streams are never blended or summed inside a window. Windows are independent, so a stream missing from one window lets the next ladder rung fill it — **gap-filling union** (Watch off overnight → Fitbit hours fill; Fitbit charging → Watch hours fill).
  - The resolver returns a **flattened winning POINT SET, never a pre-summed number** — the winning points go to the unchanged `aggregateGroup`/`rangeFromPoints`. "Day total = sum of hourly winners" is valid only for `sum` metrics and is nonsense for `minmaxavg`/`average`; the point-set design works for every aggType with zero special-casing.
  - **Ladders.** `'manual'` is rung 1 of every ladder (a user correction always outranks a device). **Cumulative** metrics (`step_count`, `walking_running_distance`, `active_energy`, `basal_energy_burned`, `active_zone_minutes`, plus the Apple-exclusive `apple_exercise_time`/`apple_stand_time`/`time_in_daylight`) = `manual > phone > fitbit > watch` — **reversed 20/08/2026** from the original "wrist beats pocket" call: live data showed the Watch is now barely worn (0-3 of 15 days carry any watch-tagged reading) and, once checked against a real Apple Health app screenshot for the same days, the phone's own stream tracks it within ~5-10% (the earlier "pocket overcounts" worry didn't hold up once verified). Fitbit fills the phone's gaps (charging, HAE not yet synced); Watch is now the last rung, not the first. **Physiological** metrics (`sleep_analysis`, `heart_rate`, `resting_heart_rate`, `heart_rate_variability`, `respiratory_rate`, `oxygen_saturation`, `skin_temperature`, `sleeping_heart_rate`) = `manual > fitbit > watch > phone` — a 24/7 device sees the whole night, and a phone cannot sense any of them, so 'phone' is a theoretical last rung. Everything else is Apple-first. **`flights_climbed` must never enter a Fitbit-first ladder** — the Air has no altimeter/barometer (the iPhone *does* have one, so that exclusion is Fitbit-specific, not a "phones can't sense this" case).
  - The `'bucket'` key is a **UTC** hour (`recorded_at.slice(0,13)`) while `computeHourlyBuckets` displays **local** hours — **not a bug**: Oslo's offset is a whole number of hours, DST included, so the boundaries are the same real-time boundaries with different labels.
  - `collapseIntraStreamMinuteDuplicates` is **`sum`-metrics only**, applied **after** inter-stream resolution — deliberately not applied to `heart_rate`/minmaxavg (a same-minute float twin moves an average by a hair but inflates a sum ~2.7×).
  - A per-metric preference table (`health_source_prefs`) was designed, **rejected by the user and never built** — the requirement was gap-filling, not a manual toggle. Don't reintroduce it without a new decision. What exists instead is a per-section **Auto / Apple / Google** display switch (`SourceToggle.tsx`; Auto = the resolver union, the others are pure single-family fetch filters).
  - **Union-not-sum proof (first real dual-source pull, 21/07/2026):** apple 2,959 + fitbit 672 with 1 overlapping hour → **3,099** displayed (`2,959 + 672 − 3,099 = 532` = the losing stream's dropped hour; `672 − 532 = 140` = Fitbit's genuine gap-fill).
- **Intra-stream workout twins (real bug, fixed 21/07/2026):** starting a Fitness-app workout makes HealthKit hold OVERLAPPING step samples (regular + workout-associated); HAE exports each with only its START time, so one stream delivered the same minutes twice as float-noise twins (20/07/2026 showed **15,362** steps vs Apple's own **5,731**; 88 of 94 minutes were twins inside ONE stream). Fixed three ways: `collapseIntraStreamMinuteDuplicates` (display backstop), the automation's **Time Grouping = Hours** (source-level: Apple dedupes before export), and a user-approved July wipe + hourly re-export (the DB is uniform hourly since 21/07/2026). Cross-validated: Apple's own dedup and our resolver produced the IDENTICAL 10/07 total (5,721) — and the earlier inter-stream double-count on that day (Watch 5,721 + Phone 4,634 in the same hours → the app displayed 10,355) replays to 5,721. Details + the full evidence table: `docs/fitbit-air-integration.md`.
- **Never destroy health rows without checking `source='manual'` first.** Manual sleep corrections never existed in HealthKit and **cannot** be recovered by re-export. Before July's approved wipe (67,626 rows across three passes) `source=eq.manual` was verified empty — that check is the reusable rule, not the wipe.
- **1000-row server cap silently truncated Week/Month views (real bug, fixed):** `fetchHealthMetricSeries` had no pagination — **PostgREST caps every response at 1000 rows server-side regardless of an explicit `.limit()`/`Range` header** (confirmed by testing). High-frequency metrics arrive ~once a minute, so a 7-day range can be 3000+ rows; with ascending order + the silent cap the response kept the OLDEST rows and today/yesterday fell off the end (Day view looked fine, Week/Month was missing the last few days). Fixed by paginating in a loop (`.range(offset, offset+999)` until a page comes back short).
- **kJ→kcal at the source (real bug):** Health Auto Export can export `active_energy`/`basal_energy_burned` in kJ depending on device locale even though every card says "kcal" — `healthAggregate.ts`'s `qtyOf()` converts (÷4.184) for those two metrics, fixing Energy Today, the Energy chart, `HealthStatsPanel` and `ActivityRings`' Move ring in one place. Workout energy was also mislabelled "kJ" when HAE sends **kcal** (`units:"kcal"`).
- **Sleep night attribution** (`healthAggregate.ts::sleepNightKey`) — a sleep session belongs to the day you WOKE UP, and the two exporters need **different handling**: an Apple string carries an explicit offset (`…+0200`), so its date part IS the local wake day (slice it, no timezone math); **Fitbit/Google Health exports UTC (`…Z`), whose raw date part can be the PREVIOUS calendar day** for a late-evening end (23:32Z = 01:32 local next day) — it must be **converted** to the local wake day. Slicing the `Z` string split one night's overlapping sub-sessions across two night keys, so they never overlap-merged and the night showed a fragment (live: the 22/07/2026 Fitbit night showed **0.8 h instead of 7.4 h**). Falls back to the stored `date` for manual entries/raw segments. **Do not "simplify" this back to a single slice.**
- **Overlap-merge is load-bearing** (`mergeSleepSessions`): exports deliver duplicate/subset windows of the same night (live example in the code comment: whole night 02:00→07:27/4.94h AND a tail re-report 03:36→07:27/3.34h with the same awake — ONE cluster, keep longest; an interrupted night is one window with `awake` counted inside it). ⚠️ Fitbit note on that function: Fitbit reports main-sleep + separate non-overlapping nap logs and different stage names (Apple `Core` ≈ Fitbit `Light` — **never merge the two stage vocabularies**); re-verify against real payloads before trusting the merge for that source.
- **No derived sleep metrics.** Both the estimated "Sleep Score" AND the efficiency % were REMOVED on explicit user decision — measured values only. If a future source exports its own score/efficiency natively, showing that value is fine; computing our own is what was rejected. Same principle killed the **basal-energy synthetic top-up** (`computeBasalEnergyDailySeries` used to fill Watch-off hours with the median of the same hour over the prior week) — the plan was always for the resolver to gap-fill Watch-off hours with the Air's own real `basal_energy_burned` instead, but `google-health-sync` never actually fetched that data type until the v1.2 fix (20/08/2026, see the Edge Functions table) — until then this metric was Apple-only and had real holes on Watch-off hours. It now gap-fills with real Fitbit data as originally intended.
- **Manual sleep entries:** Sleep has a "+ Manual" button (date + hours) for nights the Watch wasn't worn — saved as 3 rows (`Deep`/`Core`/`REM`, `source: 'manual'`) via `insertManualSleepEntry`, matching the same raw per-segment shape real Watch data uses so every render path handles it with no special-casing. The split isn't guessed blind: `estimateSleepStageProportions` averages the user's OWN historical Deep/Core/REM shares (normalized, Awake excluded) over a fixed 30-day window, falling back to Apple's published typical adult split only with no history. The trend tooltip shows which source(s) contributed to a night so a backfilled night is distinguishable.
- **`HealthTab.tsx` UI** (Apple Health-inspired, own palette): pill-navigated sections — **Overview** (hand-rolled SVG `ActivityRings` for Move/Exercise/Stand + a collapsed-by-default workouts list, each row → `HealthWorkoutDetail`), **Steps** (hourly count + distance/pace), **Energy** (active+basal stacked bars, always kcal), **Heart** (daily avg trend), **Sleep** (stage breakdown + trend, gaps for untracked nights), **Body** (weight/body-fat/BMI). There is no generic catch-all table (an "All Data" tab always rendered empty once every metric had a home, and `categorize()`/`CATEGORY_COLORS` were deleted with it). Steps/Energy/Heart/Sleep share the `PeriodToggle` (Day/Week/Month, default Week). The content column drops its `max-w-4xl` cap at `2xl:`; the right rail swaps `TrainingCalendar` for `HealthStatsPanel` (short, **plain-computed, no AI** — averages, best/worst day, trend vs the previous week).
- **Headlines follow the SELECTED PERIOD, not just the anchor.** Day → that day's own figures ("Steps Today" / "Steps · Wed, 2 Jul"); Week/Month → "· Weekly/Monthly Average": Steps = avg/day + total steps + total km; Energy = avg kcal/day + avg active/basal (from the same chartData the bars show, no extra queries); Heart = avg of daily avgs + a period min–max chip + avg resting/HRV; Sleep = avg h/night + an AVERAGED stage bar + "N nights tracked · best/lowest" — **never a single night pretending to be the period**. In Day mode the range queries collapse to `from==to==anchor`, so React Query dedupes them with the anchor-only fetches.
- **Date navigation** (`DateNav.tsx` + `dateNav.ts`): every view shows the exact date/range (e.g. "1–7 Jul (this week)") with prev/next arrows and a native date-picker; `rangeForAnchor`/`stepAnchor`/`labelForAnchor` compute the window from an `anchor` (defaults to today, resets on period change, never steps past today). **Stale-anchor bug (real, fixed):** `anchor` was plain `useState(todayStr())` — with the tab open across midnight, "today" moved on but the anchor didn't, so a section silently froze on whatever day it was first opened, and since each section has its own anchor two sections could show two different stale dates (confirmed against live data: fresh same-day rows in the DB, UI days behind). Fixed with `useAnchorDate()`: re-checks on focus/visibility/a 60s interval and advances **only if** the anchor still equals the previously-known "today" — an intentional look-back is left untouched.
- **Chart interaction contract** (one rule, all Health charts): **hover shows values immediately** (no `trigger="click"`); the ONLY things that navigate are explicit links inside the tooltip ("Go to this day →", or "Correct manually" for Sleep) — a bar click never navigates. Tooltips need `wrapperStyle={{ pointerEvents: 'auto' }}` or those links are unclickable. Trend-bar **actions** live in a pinned popover anchored AT the clicked bar (`{date,x,y}` from the Bar's own onClick coords, clamped to the wrapper) — an action strip below the chart was explicitly rejected. Steps/Energy also wire drill-down straight on their own `Bar` (`BarLineChart` exposes `onPointClick`). Steps' ugly default tooltip (`": 514 steps"` — empty series name + colon) uses a custom content renderer like the others.
- **Chart click "frame" (real bug, THREE rounds — root cause found by E2E, generalizable gotcha):** clicking any Health chart drew a black rectangle around it. Rounds 1–2 (focus-outline CSS + `cursor={false}`) were the right ideas but **the CSS never shipped**: the `.recharts-*:focus { outline: none }` rules sat inside `@layer base`, and **Tailwind tree-shakes custom rules inside `@layer` blocks whose selectors never appear in source files** — `.recharts-surface` only exists at runtime inside the library, so the rule was silently purged from every build (verified: zero recharts rules in the served CSS; the focused `svg.recharts-surface` computed `outline: auto 5px`). recharts v3's accessibilityLayer gives that svg `tabIndex=0`, so every click focused it → browser focus ring = the frame. Fix: the rules moved OUTSIDE all `@layer` blocks (bottom of `index.css`). **Rule of thumb: any CSS targeting library-runtime class names must live outside `@layer`.** `cursor={false}` on every Tooltip stays.
- **Canonical chart style** (default for any future Health chart): a translucent `Bar` + a `Line` with dots on top, same colour, via the shared `BarLineChart.tsx`. `rangeKey` (optional min–max band) exists but only `HealthWorkoutDetail`'s HR curve uses it. **Tooltip showed the average twice (real bug):** `BarLineChart` renders a Bar and a Line on the same `dataKey`/`name` (the whole point of the style) and recharts' default Tooltip shows one row per graphical element — fixed with a custom `content` renderer that dedupes by `dataKey`.
- **Mini-metric grid** (`MetricMiniCard`/`MetricMiniGrid`/`miniMetrics.ts`): every HealthKit metric without a full chart gets a small card (max 4 per row: `grid-cols-2 sm:grid-cols-4`, never more) below its section's main widget — value + Today/7-day-avg/Latest (per the metric's `AggType`) + a one-line explanation. Grouped onto the closest section: Steps (mobility/gait + activity extras + Running Dynamics), Energy (nutrition), Heart (cardio recovery + VO₂ Max), Sleep (respiratory rate, wrist temp), Body (lifestyle & environment). `showTodayCount` adds "N× today"; `showTodayTimes` lists occurrence times (handwashing/toothbrushing). `push_count` ("wheelchair pushes") is very likely misdetected for non-wheelchair users — continuous per-minute fractional samples all day is not what discrete pushes look like; the card points at Watch Settings → Accessibility → Wheelchair, not at our aggregation.
- **`HealthWorkoutDetail.tsx`** — each `health_workouts` row stores the FULL Health-Auto-Export payload in `raw` (jsonb) but only summary columns used to be shown; the per-interval **HR curve** (`heartRateData`), **GPS route** (`route[]`), cadence, distance, avg/max speed, pace, elevation, weather, HR recovery and step count were all ingested then dropped. The detail surfaces all of it from `raw` client-side (no schema change): a `BarLineChart` HR curve using `rangeKey` for the min–max band + a self-drawn SVG route polyline (green start / red end — **no map tiles, CSP blocks external hosts**) + a stat-chip grid. Never-populated cards (gait/nutrition) are unlogged data, not a hardware gap. SpO₂/skin-temp are no longer a hardware gap either as of 20/08/2026 (Watch upgraded SE 2 → Series 11, which has both sensors) — they stay empty until a real Health Auto Export payload confirms the exact field names (see `docs/fitbit-air-integration.md` §5a); don't assume the old "Watch physically can't" reasoning still applies to any card.
- **Health Auto Export operational facts:** importable automation configs live in `docs/health-auto-export/`. Required app settings, confirmed against production data: **Export Version 2** (v1 is legacy — no `id` field, different field shapes), **Summarize ON**, **Time Grouping: Hours**, **all Health Metrics enabled** (not a curated subset), **Date Range "Since Last Sync"** for the recurring automation with a separate weekly "Previous 7 Days" reconciliation automation, **Batch Requests ON** once route data / many metrics / fine grouping are enabled. If a workout arrives without `id`, the webhook falls back to a deterministic key (`name+start+end`) instead of dropping it — still idempotent, just not a real UUID; only a workout with no id AND nothing to build that key from is skipped (counted in `skipped_workouts`). Numeric workout fields (`duration`, `activeEnergyBurned`, `totalEnergy`, `heartRate.avg/min/max`) arrive as either a plain number or a `{qty, units}` object depending on the field — `numOrQty()` unwraps either. Known gap: `stateOfMind`/`medications`/`symptoms`/`cycleTracking`/`heartRateNotifications` payload arrays are not ingested. **ECG** (`HKElectrocardiogramType`) status is more nuanced, researched 20/08/2026 after the user's Watch upgraded SE 2 → Series 11 (which has the ECG electrodes; the SE line never did): Health Auto Export's own docs claim it exports a full record (`{start,end,classification,severity,averageHeartRate,numberOfVoltageMeasurements,voltageMeasurements,samplingFrequency,source}`) rather than only a classification string, but this contradicts a second doc source from the same vendor and was NOT verified against a real export — treat as unconfirmed until one real ECG recording (run manually on the Watch) round-trips through the webhook. If it does arrive, the existing generic metric ingestion needs no code change to store it; a dedicated UI view would. **Auth:** researched across several passes against the vendor's own docs — no HMAC/request-signing exists for this integration; Bearer token over HTTPS is the documented and only supported model, so `Authorization: Bearer <secret>` is correct, not a workaround. Batch-split behaviour (size/count triggers, ordering, partial-failure semantics) is undocumented, so the webhook deliberately depends on none of it: every request is processed independently and idempotently. Platform reminder: HealthKit is unreadable while the phone is locked and background scheduling is opportunistic — delivery is best-effort by OS design, not a bug in either app.
- **Google OAuth token posture (operational, verified hands-on 21/07/2026):** the consent screen in **Testing = 7-day refresh token; Production (even unverified) = effectively non-expiring** — publishing to Production is mandatory for background sync. Restricted `googlehealth.*` scopes on an External + Production + unverified app **do** show the "Google hasn't verified this app → Advanced → continue" click-through. **A re-consent can omit a fresh `refresh_token` unless `prompt=consent` is forced** — so a scope-union re-consent can leave the UI reading "connected" while the stored token still covers only the old scopes; verify with Google's `tokeninfo` endpoint after any scope change. Fallback if the posture ever fails: an **Internal** Workspace app (no cap, no verification, no 7-day limit) at the cost of employer-tied identity. Full detail: `docs/fitbit-air-integration.md`.

### AI (ai-proxy) Detail

Gemini via the `ai-proxy` edge function. Three call modes: the conversational **tool-calling loop** (default), **`responseSchema`** → `callGeminiStructured` (single-shot, `responseMimeType: 'application/json'` + a Gemini-format schema, returns `{ data }`, no tools/no loop — structured extraction, not chat), and **`{ fetchUrl }`** (server-side page fetch because the browser can't hit arbitrary origins under CORS; SSRF guard blocks localhost/private ranges, 8s timeout).

**Generic DB tool layer** over a curated `DB_CATALOG` allow-list: `describe_database`, `db_query`, `db_insert`, `db_update`, `db_delete`, **`db_aggregate`**. Guardrails: default-deny allow-list (token/secret/auth tables unreachable), `access: 'rw'|'ro'` per table (hevy_*/strava_activities/health_metrics/health_workouts/movies/tv_series are read-only — synced externally), **every op force-scoped to `user_id` server-side** (the model never supplies it), no DDL, update/delete refuse empty filters, 16-turn loop cap. `app_error_logs` is readable so the AI can help diagnose failures. `pt_assessments`/`work_notes`/`work_weekly_goals`/`work_pinned_links` are read-only; **`dev_requests` and `wish_items` are read-write on purpose** — the user dictates backlog notes and wishes in chat (and by Siri) and the AI writing a structured row there IS the intended flow; `wish_items`' catalog `rules` carry the never-a-deadline guardrail and the four-way tie-break against `dev_requests`/`ai_memory`/`shop_items` (see Wishes). **Behavioural guardrails in the system prompt:** announce what it's about to do before any create/update/delete, and require **explicit user confirmation before ANY delete** (never auto-deletes). `DB_CATALOG` stays behind `describe_database` (on-demand, not in the prompt). **Gap:** `water_log_entries` (migration 067) is NOT in the catalog — the AI cannot see water logs.

- **`db_aggregate`** — `sum|avg|min|max|count` + `group_by`; enum'd ops only (never expressions), the same filter grammar as `db_query`, group columns validated against the catalog, hard cap ≤366 groups, rows reduced server-side so only the summary reaches the model. This is what makes "no pre-written query per scenario" true for analytics. **Tool-choice ladder** in the system prompt: current value/list → `db_query` · average/trend/compare → `db_aggregate` · whole-day picture → `get_day_summary` · "that thing I saved" → `semantic_search` then `db_query` by id · writes → `db_insert/update/delete` with the confirm rules · external world → the special tools.
- **`semantic_search`** (migration `065`: pgvector + `ai_embeddings` `vector(768)` hnsw cosine + the `ai_semantic_search()` SECURITY-INVOKER RPC) — `gemini-embedding-001` @768, **cosine index → no manual normalize**, vector `limit ≤ 8`, `user_id` filtered inside the vector query; returns `{source_table, source_id, snippet, score}` and the model then `db_query`s the live row by id. **Embeddings locate, live SQL answers — never use a similarity hit as the factual value of anything current-state.** Reindex is user-triggered (Developer → "Reindex AI search"), not embed-on-write. Caveat: the embedding request shape was confirmed from docs, **not a live call**.
- **`run_read_query`** — the opt-in read-only live-SQL escape hatch. **Its security boundary is the DATABASE, not the edge regex:** ai-proxy's normal ops run under the service role (RLS bypassed), so this path instead builds a **user-JWT client** and calls a **SECURITY INVOKER** RPC (`ai_run_read_query`) that runs as `authenticated` (RLS applies), sets `transaction_read_only = on` + a 5s `statement_timeout`, and wraps in `LIMIT 500` before `jsonb_agg`. Containment = RLS + the read-only transaction + migration 044's revoked OAuth-token-table grants + the hard row cap. The edge guard is a best-effort UX filter that **fails closed** (strip comments first, reject quoted identifiers, `SELECT`/`WITH` only, single statement, block `pg_`/`information_schema`/other schemas, check every `FROM`/`JOIN` target incl. comma-joined lists against the catalog, recognise CTEs only as `name AS (`). **The broad DML-keyword regex was deliberately REMOVED — do not re-add it**: it false-rejected reads containing "update" inside a string literal; write-prevention is the read-only transaction's job.
- **Special-purpose tools** generic CRUD can't cover: `get_calendar_events` · **transit** — `plan_trip`, `get_next_transit`, `get_saved_transit`, `search_transit_stops`, `save_transit_stop`, `save_transit_route` · **media** — `get_media`, `plan_media`, `mark_episode_watched` · **health** — `get_health_stats` · **shop** — `get_shop_categories`, `create_shop_category`, `create_shop_item`, `ask_clarifying_question` · **recipes** — `get_recipes`, `create_recipe` · **training** — `update_hevy_routine`, `create_hevy_routine` (both forward to `hevy-api`), `get_athlete_profile` (see Training's athlete-profile note) · `get_day_summary` · `save_memory` (+ rw `ai_memory`: durable notes the user asks the AI to remember, recalled via `db_query`).
- **`ai_memory` — an "ABOUT ME" context block + a management surface.** Two gaps closed together on user request. (1) `aiApi.buildContext` now appends one more query (last in the array, so no existing `get<T>(i)` index shifts) reading `kind IN ('fact','preference')` only — deliberately **not** `note`/`summary`, which stay on-demand-only via `db_query`/`semantic_search` so they never bloat every single turn's context — capped at 12, newest first, rendered as an `ABOUT ME` block right after `DATE`/`TIME` (before `TODAY'S TASKS`): durable identity facts colour how everything else should be read, so they go first. (2) `save_memory`'s tool description now explicitly covers TWO distinct triggers, not one: remembering a single fact/preference (unchanged), and an explicit **"summarize this conversation"** request ("topla ve not al", "bunu özetle", "summarize this conversation", …) — compact the WHOLE conversation so far into one `kind="summary"` call, not just the last message. (3) A new Developer → **Memory** tab (`MemoryTab.tsx`/`MemoryRow.tsx`/`MemoryEditSheet.tsx`, `src/features/ai/api/memoryApi.ts`) lists/edits/deletes every row and is the only path that ever writes `source='user'` (every other row is `'ai'`, from `save_memory`, or `'auto'`) — before this, `ai_memory` had no UI at all: nothing to see what was remembered, nothing to correct a wrong entry. (4) `AIPanel` gained a 📝 header button, gated on `messages.length > 0`, that sends a normal chat message ("Summarize this conversation and save it to memory." / the Turkish equivalent, following the existing voice-chat TR/EN chip) down the same `handleSend` path as any other turn — the model's `save_memory` call still lands in the transcript like any other tool use, nothing hidden.
- **`get_health_stats` — two fixes worth remembering.** (1) It and the catalog both pointed at `health_daily_stats`, a table dropped when migration 041 moved Health to point-in-time grain, so the AI's health answers were silently broken (empty/stale) ever since; rewritten to query `health_metrics` directly and aggregate with the same rules as `healthAggregate.ts` (sum steps/energy, min/max/avg heart rate, latest resting HR, kJ→kcal). (2) **It now returns SLEEP too** — `computeSleepNights` (absolute-time overlap-merge keep-longest, Oslo wake-day attribution, per-night hours + deep/core/rem/awake, last night + period average). That closed the "no sleep data" answers in the phone `sleep` action and the Coach. The prompt tells the AI to *analyze* health data (trends, comparisons, honest criticism), not recite numbers.
- **Latency design for `plan_trip` (was a real 3–4 min pain point).** It is a ONE-SHOT call: the prompt and tool description tell the model to call it directly with the user's own words (`from:"ev", to:"iş"`) and NOT to look things up first — earlier "investigate first" wording caused ~5 sequential Gemini turns (each a full round-trip) vs ~2 now. `plan_trip` resolves both endpoints itself against saved stops/routes loaded ONCE (not re-queried per endpoint), in parallel: empty → default saved stop; an exact `NSR:StopPlace:` id → used as-is (name backfilled from the trip legs, no extra lookup); "home"/"ev"/"work"/"iş" (+variants) → saved-label fuzzy match; else → geocoder top match. When a side won't resolve it returns `{success:false, needs_clarification:true, candidates:[…]}` so the model asks ONE `ask_clarifying_question` instead of firing more search turns. **Only a regex-validated `NSR:StopPlace:` id or finite coords are ever interpolated into GraphQL** (injection safety). Anti-hallucination is structural (report only what the tool returns; call once and report), not extra verification turns. New EnTur GraphQL fields are deliberately NOT added speculatively — verify live first.
- **Model chain + retries.** Gemini returns transient "model overloaded" 5xx that clear within seconds, distinct from a 429 (real quota — surfaces immediately via `throwRateLimit`). Overload waves hit different models' capacity pools unevenly, so retrying a DIFFERENT model beats retrying the same one. `fetchGeminiWithFallback` tries `MODEL_CHAIN` = `gemini-3.5-flash` → `gemini-3.1-flash-lite` → `gemini-2.5-flash` → `gemini-2.5-flash-lite` with jittered exponential backoff, position-weighted attempts (`RETRIES_BY_POSITION` [4,3,2,2]) so better models are tried hard before falling to lite tiers. **Every model id must be live-verified before entering this list** (real bug: two guessed ids 404'd in production; a 404 also used to abort the whole chain — it now skips to the next model). Probe with ai-proxy's `{listModels:true}` debug branch, which returns the key's REAL available models. The client can pass a `model` — it becomes the chain's **preferred starting point**, not an exclusive choice (`AIPanel`'s header picker, localStorage `lasci-ai-model`, "Auto" by default); `AIResponse.model` reports which model actually answered.
- **`thinking_config` per-model body (real bug, round 3):** `thinking_config`/`thinking_level` is a `generationConfig` field that is **Gemini-3.x-only** — explicitly picking `gemini-2.5-flash` 400'd with "Thinking level is not supported for this model" the moment a real user tried it, because ONE fixed request body was built once and reused across every model in the chain. `fetchGeminiWithFallback`'s `body` param became **`buildBody: (model) => AnyRecord`** — the body is constructed PER MODEL inside the retry loop, and `supportsThinking(model)` (`model.startsWith('gemini-3')`) gates whether `thinking_config` is included. Applies to all three call sites (the tool loop, the turn-limit summary, `callGeminiStructured`). `thinking_level: MINIMAL` in the loop — thinking tokens are not the cost leak.
- **Prompt-caching contract (don't regress it).** Live context used to be baked into `systemInstruction`, which made Gemini's implicit prefix cache (~90% off a repeated byte-identical prefix) structurally unable to hit — a ~7,270-token fixed block (system + serialized TOOLS) was billed at full price on every turn of every tool loop. Now: **`SYSTEM_PROMPT` and the per-surface tool declarations must stay byte-identical per call** — never interpolate a date, timestamp or any volatile value into them (instant cache-buster; the current date belongs in the context turn), and serialize tools deterministically. Live context rides as `contents[0]` (`prependContext` in `aiApi.ts`). `buildContext` is skipped for clearly transit-only questions (a conservative client-side `TRANSIT_RE` gate). **`ai_usage_log.cached_tokens`** (← `usageMetadata.cachedContentTokenCount`) is the proof metric and the documented trigger for escalating to explicit `cachedContent`. `ai_query_log` records every `db_query` + live SQL with the exact statement — the substrate for later promoting hot queries into the UI.
- **Tool slicing + model routing.** Slicing is deliberately narrow: **only the bounded `shop` surface is sliced, and the `phone` surface gets NO tools at all** (its brief context is pre-built server-side by `phone-gateway`, so a single-shot answer is the biggest latency win) — **coach and general keep the FULL tool set** so the assistant is never weakened. `SURFACE_MODEL` changes only the **starting** model: `shop: 'gemini-2.5-flash'`, `phone: 'gemini-3.1-flash-lite'` (skips the frequently 503-overloaded 3.5-flash primary, which burned ~7–13s of retry budget before falling through — fatal against the ~25s iOS Shortcuts cutoff). A 503 still falls through the whole chain.
- **Voice chat (`useVoiceChat.ts` + AIPanel):** browser-native Web Speech API — `SpeechRecognition` (prefixed `webkitSpeechRecognition` everywhere that has it) for speech→text and `speechSynthesis` for text→speech. **No key, no edge function, no cost** — nothing server-side changed. Two levels: the 🎤 button dictates into the box (edit before sending), and the header **🎙 Voice** toggle is hands-free mode — utterance auto-sends, the reply is spoken, and the mic reopens when it finishes (`speak(text, onDone)` → `startListening()`), so it's a real back-and-forth loop. TR/EN language chip (`lasci-ai-voice-lang`) drives BOTH recognition and the picked voice. Load-bearing details: `speakableText()` strips `**bold**`/emoji so the voice doesn't read markup; `startListening` cancels any speech first (otherwise it transcribes its own voice); **`primeAudio()` must run inside the enabling tap** — iOS refuses synthesis that no user gesture unlocked, and the reply is spoken much later; the panel's close paths reset voice mode and `voiceModeRef` is `voiceMode && isAIOpen` so the loop can't continue behind a closed panel; a silent utterance ends the loop rather than reopening a hot mic. Everything is feature-detected — the controls simply don't render where the API is missing. **Recognition is unreliable inside an iOS home-screen PWA** (a long-standing WebKit gap); on the phone the `AI'a Sor` Shortcut is the voice path (Siri dictation → gateway → Speak Text), so this mainly serves desktop.
- **Vision:** `Message.images` end-to-end via a shared `messagesToContents()` that attaches base64 photos as Gemini `inline_data` parts (chat + structured). `AIPanel` supports the 📷 attach button (≤3, thumbnails, image-only send) **and pasted images** (Cmd/Ctrl+V on desktop, paste on mobile — `onPaste`); `parseFoodPhoto()` + `fileToCompactDataUrl` downscale before upload.
- **MCP was evaluated and deliberately not pursued:** GitHub Pages is a static host and cannot run a persistent process, so any MCP layer would need a separate always-on server. The generic `DB_CATALOG` + `db_query/insert/update/delete/aggregate` layer shipped instead, and the "AI proposes, user confirms" invariant lives in the system prompt.
- **Removed / never built:** dead `log_workout`/`get_workouts` (hit the dropped `training_sessions` table — training is Hevy/Strava, read via `db_query`; planned sessions = `time_blocks` `category='training'`). The **Weekly Review** Home card added during the AI phases work was **removed on request** — `weeklyReviewApi`/`useWeeklyReview`/`WeeklyReview` no longer exist in `src/`, and `ai_reviews` (migration 065) is an unused empty table left in place.
- Cost model, security invariants, the architectures evaluated and the pricing snapshot: `docs/ai-cost-capability-analysis.md`.

### iPhone surface (Shortcuts · Scriptable · Web Push)

**`phone-gateway`** is the single durable entry point (see the Edge Functions table for auth). **11 actions:** deterministic `log_supplement` · `log_food` · `log_water` · `nutrition_today` (includes `water_ml`) · `recent_foods` · `search_library` · `sleep_stats` · `tasks_today`; AI `ask` · `brief` · `sleep` (forwarded to `ai-proxy` with the same secret). The action table in `docs/iphone-examples.md` is **the contract** — Shortcuts and widgets must match it.

**The ~25s design constraint (why the AI actions look the way they do):** iOS Shortcuts' "Get Contents of URL" times out at roughly 25s, which is short for a tool-looping LLM. Three deliberate choices, all in `phone-gateway`/`ai-proxy`:
1. **`FAST_MODEL = 'gemini-3.1-flash-lite'`** starts every AI action, skipping the frequently 503-overloaded 3.5-flash primary; ai-proxy still falls through the rest of the chain.
2. **Single-shot, not a tool loop** — `brief` pre-builds its context SERVER-SIDE (`buildBriefContext`: today's tasks + schedule + upcoming training, service-role queries explicitly user-scoped, best-effort per section) and sends `surface: 'phone'`, which `toolsFor()` maps to **no tools**, so Gemini answers in one turn. Gemini rejects an empty tools array, so the key is dropped from the body entirely.
3. **A 22s `AbortController`** on the ai-proxy call returns a clean error BEFORE the client cutoff instead of a silent hang.

**Apple Shortcuts generator** — `scripts/iphone-shortcuts/` (**Codex-owned, do not edit**): `generate.mjs` emits 11 shortcuts — `Log Creatine`, `Su İç`, `AI'a Sor`, `Sabah Brief`, `Beslenme Durumu`, `Uyku Özeti`, `Uyku İstatistikleri`, `Bugünün Taskları`, `Barkod Tara`, `Atıştırmalık Logla`, `Akşam Yemeği Logla`. Rules: the device secret is written as the `__PHONE_GATEWAY_SECRET__` placeholder — **never commit a signed `.plist`/`.shortcut` containing the real secret**. Cross-app contract: **`Barkod Tara` scans with iOS's built-in barcode action, then opens `scriptable:///run/Yemek%20Logla?ean=<code>`** — so the Scriptable script MUST be named exactly `Yemek Logla` and read `args.queryParameters.ean`. Spec + task board: `docs/codex-shortcuts.md`.

**Scriptable** — `docs/scriptable-food-logger.md` (the `Yemek Logla` mini app: recents / own library / Open Food Facts / scanned barcode) and `docs/scriptable-widgets.md` (4 home-screen widgets — `Uyku Paneli`, `Komuta Merkezi`, `Hizli Log Paneli`, `Makro Halkalari` — plus the `HizliLog` runner). **Script names are load-bearing**: deep links resolve by name.

**Web Push (lock-screen morning brief)** — migration `068` creates `push_subscriptions` + the pg_cron job `lascis-morning-push` (`'0 7 * * *'` = 07:00 UTC ≈ 09:00 Oslo in summer / 08:00 in winter), which calls `push-send` via `net.http_post` with `x-cron-secret` read from Vault inside the cron (never in the SQL text). Client: `usePushNotifications` + a toggle in Settings (⚙) → Notifications. The SW push/notificationclick handlers live in **`public/push-sw.js`**, pulled into the Workbox service worker via `vite.config`'s `workbox.importScripts` — a separate file so the generated SW isn't hand-edited. Setup runbook: `docs/web-push-setup.md`. See Platform & Shell for the hard iOS Web Push constraints (installed PWA only, visible notification required).

---

## Not done yet

The **live backlog is the in-app Dev Requests drawer** (`dev_requests`). This list is only the coarse long-term items that outlive a single session.

- **Football data source** — no route, and the `football-api` edge function is no longer in the repo. Calendar-based fixtures are the intended path.
- 🔶 **Exercise demo GIFs** — (a) optional true self-host: mirror ExerciseGymGifsDB into Supabase Storage and swap the single `GIF_SOURCE` constant (still the public jsDelivr CDN today); (b) a manual override map for titles the 0.5-threshold fuzzy matcher misses (none exists — `exerciseMedia.tsx` is fuzzy-match only).
- 🔶 **Wishes — deliberately NOT built, don't rebuild it:** no `wish_periods`/`contexts` grouping table · no recurring MM-DD windows, wrap-around resolver or `lead_days` (periods are concrete dates) · no seasonal reset and no annual review prompt · no `lat`/`lon`, map, geofence or proximity trigger (CSP blocks map tiles, the only wired geocoder is EnTur/Norway-only, and `useGeolocation` returns the Oslo fallback as a *success* — a denial would read as "he is in Oslo") · no SQL view and no generated column · no new AI tool, `phone-gateway` action or Shortcut · no 7th glance-board cell and no 5th Daily mode tab (that control is `grid grid-cols-4`). Note that the `windowChips` season chips are the **only** practical way to capture a period — `DateInput` has no calendar popup, so a typed range costs 16 digit taps against a chip's one; don't remove them in favour of date boxes.
- 🔶 **Finish migrating mutations to `useMutationWithFeedback`** — currently 57 bare `useMutation(` vs 27 migrated in `src/features`. `useTodos.ts` is still bare for create/update/toggle/delete (only `useSwapTaskOrder` is migrated) and must not be migrated on its own — `UnifiedPlanModal.handleSave` already wraps those four in a full `toast.loading/success/error` block, so a hook-only change double-toasts the app's highest-traffic path; calendar, media, projects, recipes, shop, work, training and `home/useGames` all have bare hooks. `useTransitRoutes`/`useTransitStops` are plain async functions, not mutations. **Note:** `useSchedule.ts`'s two create hooks carry in-code comments recording a deliberate decision NOT to migrate (their consumers already toast) — that's a decision, not a TODO. See Coding Rules → Toast feedback for the migration rule.
- 🔶 **AI media coverage** — the media tools ship (`get_media`/`plan_media`/`mark_episode_watched`, and the catalog documents `rating`, `personal_note`, `status`, `current_season/current_episode`), but: `genres (jsonb)` is absent from the catalog columns; `get_media` returns only `entry_id/title/status(+S/E)` (no rating, genres or dates); and `aiApi.ts`'s `SYSTEM_PROMPT` has **no MEDIA persona block** (just one tool mention) unlike HEALTH/TRAINING/NUTRITION. Also missing from the prompt: "Coming soon is date-derived, not a status".
- **Activity Log / stats widget** (user-facing feed, if ever built) — it must be **immutable append-only**: SELECT + INSERT policies only, and the **absence** of UPDATE/DELETE policies is the enforcement mechanism. Sketched event vocabulary: `task_created`, `task_updated`, `task_completed`, `task_cancelled`, `media_added`, `media_status_changed`, `media_planned`, `media_completed`, `media_dropped`, `ai_action_proposed`, `ai_action_confirmed`, `ai_action_cancelled`, `calendar_event_synced`. **`audit_logs` (migration 037) already covers the CRUD audit trail — don't rebuild that**; this would be a user-facing feed, not an audit table.
- **Fitbit / Google Health follow-ups** — tracked in `docs/fitbit-air-integration.md` § "Open items": the ≥7-day refresh-token survival formality, a daily reconciliation pull inside the **14-day** query-range cap (an outage longer than that is permanently unrecoverable — there is no backfill), a stale-data banner, the webhook-push decision, a Body-section source toggle, a Source Lab / compare view, hypnogram verification, the unconfirmed **SpO2 shape** (dual-path A/B ships with an `oxygen_saturation_source` counter), the HRV field per platform (Apple SDNN vs Fitbit RMSSD), and one click to confirm Calendar still works after the unified re-consent.
- **Home Routes widget** — one more visual polish pass (the refresh button shipped).
- **Kassalapp** — confirm the Norwegian `display_name`/`code` values and the per-100g basis against ONE live sample (see Food).

---

## Coding Rules (Strict)

### Mobile-first — MANDATORY
Every component mobile-first. `min-h-[44px]` on every interactive element. No exceptions.

```tsx
// ✅
<button className="w-full md:w-auto min-h-[44px]">
// ❌
<button className="w-32 h-8">
```

Hover-only actions need an always-visible mobile fallback. No horizontal page overflow.

Modal pattern — use `@headlessui/react` Dialog (handles Escape, focus trap, portal automatically):
```tsx
import { Dialog, DialogPanel, DialogBackdrop } from '@headlessui/react'

<Dialog open={isOpen} onClose={onClose} className="relative z-[60]">
  <DialogBackdrop transition className="fixed inset-0 bg-ink-900/30 transition duration-200 data-[closed]:opacity-0" />
  <div className="fixed inset-0 flex items-end sm:items-center justify-center p-0 sm:p-4">
    <DialogPanel transition className="w-full rounded-t-2xl sm:rounded-2xl sm:max-w-md max-h-[90vh] overflow-y-auto bg-cream-50 border border-ink-200 transition duration-200 data-[closed]:opacity-0 data-[closed]:translate-y-4 sm:data-[closed]:translate-y-0 sm:data-[closed]:scale-95">
      {/* content */}
    </DialogPanel>
  </div>
</Dialog>
```

### Toast feedback — MANDATORY
Every async action must show feedback on completion. Toasts appear bottom-left.
🟢 success · 🔴 error · 🟡 warning · ⚫ loading.

**For any new TanStack Query mutation, use `useMutationWithFeedback`**
(`src/shared/hooks/useMutationWithFeedback.ts`) instead of bare `useMutation`:
```ts
return useMutationWithFeedback({
  action:         'delete_time_block',   // logError context — the only required extra field
  successMessage: 'Deleted',             // optional — omit for silent-success on in-place edits
  mutationFn:     (id: string) => deleteTimeBlock(id),
  onSuccess:      () => qc.invalidateQueries({ queryKey: ['schedule'] }), // your own logic still runs
})
```
This exists because "every async action must show feedback" was hand-copied per call site —
inconsistently applied and easy to forget (real bugs: the daily timeline's drag/postpone/rename/
delete and the to-do list's toggle/delete/reorder had **zero** feedback on failure; two
Developer-tab "clear logs" mutations toasted but never called `logError`, so a failure to clear
the error log was invisible in the error log itself). `useMutationWithFeedback` bakes the
guarantee into the mutation primitive — like a return-code check that can't be skipped: error is
**always** toasted and logged to `app_error_logs`; success is silent by default (matching the
"edits feel live" convention) unless `successMessage` is given.

**Migration rule for the remaining bare hooks:** several are called from multiple components,
some of which already wrap `mutateAsync` in a complete `toast.loading/success/error` block.
Switching the hook alone would double-toast those. Migrate the hook **and** simplify every call
site to drop its local toast duplication in the same pass — do it the next time one of those
files is touched for another reason, not as a big-bang rewrite.

For genuinely one-off async actions that aren't a TanStack mutation at all (a button
handler calling an API directly), the manual pattern is still fine:
```ts
const tid = toast.loading('Saving…')
try {
  await doSomething()
  toast.dismiss(tid); toast.success('Saved ✓')
} catch (err) {
  toast.dismiss(tid); toast.error((err as Error).message ?? 'Failed')
}
```

### Reference viewport sizes (design/testing baseline)
Get the current size in the browser console with `window.innerWidth + 'x' + window.innerHeight`:
- **Mobile**: 852×393 · **Laptop**: 1469×680 · **Monitor**: 2450×1130

### Layout width — content-sized, left-aligned (MANDATORY)
Never stretch content edge-to-edge. Widgets are sized to their content, not the viewport. The complaint that produced the numbered rules below: tiny data stretched wall-to-wall on big monitors. They make "content-sized" **measurable** so it never regresses. (Piloted on Training; roll out page-by-page — Home → Media → Work — only on explicit request per page.)

- Page wrapper: `w-full px-4 sm:px-6 lg:px-8` (left indent, NOT `mx-auto` centered). Page background is `bg-canvas` (#EDE4D5, soft warm cream); cards sit on `bg-cream-50` for contrast.
- Each content block gets a `max-w-*` capped to what it needs, **left-aligned** — leftover horizontal space stays on the right. Small cards flow side-by-side (grid/flex-wrap), never stack full-width.
- **W1 · Container-relative, not viewport.** A widget adapts to its OWN box width — use `@container` + `@sm/@3xl/@4xl:` variants (the `@tailwindcss/container-queries` plugin is installed), not `sm:/lg:` on inner widgets. The same component can then be dense in a narrow slot and rich in a wide one.
- **W2 · Column flow, fixed widths.** List/card collections flow into content-width columns via `grid-cols-[repeat(auto-fill,minmax(MIN,MAX))]` + `justify-start`; the column COUNT derives from available width, rows never stretch. Caps: dense one-line rows `15–18rem`, cards `19–22rem`, large cards `24–28rem`. A single content row must never exceed **40rem (640px)**. (Exception: `TodaySummary` uses explicit `grid-cols-*` steps, not auto-fill — positional stability outranks column flow there.)
- **W3 · Controls are content-sized.** Text/search inputs `≤ max-w-md` (512px); info banners `w-fit`; a one-line control is never full-width. A 1900px search box is a bug.
- **W4 · Charts: aspect-locked + capped.** A chart keeps its aspect ratio and caps by role (small trend `max-w-2xl`, primary `max-w-4xl` = 896px). Growing a chart = grow proportionally, never smear to full width.
- **W5 · Detail on demand.** Secondary info (GIFs, history, explanations) lives in a hover/tap peek, not permanent screen area (desktop hover + mobile tap, per the mobile-fallback rule).
- **W6 · Measurable.** Verified at 393 / 1469 / 2450px: no horizontal overflow at any width; at 2450px no single-line content > 640px, no auto-fill column > 384px, no text input > 512px, no chart svg > 896px. Re-check these numbers when applying the standard to a new page (an audit script was used in-session but was never committed — write a throwaway one under `scripts/` per the no-unit-test-framework convention).
- **Exception — data-dense dashboards on large monitors:** a `max-w-4xl` column next to a fixed side rail leaves a dead zone on 2xl+ (2450px-class) monitors. For these (e.g. Training → Health) drop the cap at `2xl:` (`2xl:max-w-none 2xl:flex-1`) so the column fills the remaining flex space; grids inside can add a `2xl:grid-cols-*` step. Still content-sized below 2xl.
- **Density toggle was tried and REJECTED** (Comfortable/Compact/Dense) — it changed the VERTICAL plane; the complaint was horizontal. Don't reintroduce it.

### `NEVER_HIDES` — a new surface adds rows, never subtracts them (MANDATORY)
**The user's explicit decision, and the reason Wishes is its own table.** No wish/period/window/`kind` predicate may be added to an existing task query — not `fetchTasksForDay`, not `fetchAllTasks`, not `fetchTasksByWeek`, not `briefingApi`/`buildBriefContext`, not `phone-gateway`'s `tasks_today`, not `push-send`'s task query. New surfaces (the Wishes page, the `DayView` resurfacing row, the morning-push segment, `TasksPanel`'s "Open now") **only ADD**. In his words: a season "is only the app's note about when to bring it up by itself; it is never a rule about when you are allowed to see it… The only thing a season changes is when the app speaks first." A row written in July stays readable, searchable and askable-about in July.

The **one** sanctioned exception is the `fetchTasksForDay` `dueFilter` fix (Daily → the `section` day-leak): it removes duplicate *wrong renderings* of a row, never the row.

### Other rules
- **English-only project artifacts (MANDATORY, user rule 2026-07-21):** everything in the repo or on GitHub — code, comments, UI strings, error/toast messages, commit messages, PR titles and bodies, docs — is written in English. The ONE exception is generated **on-phone** Shortcut/widget user-facing strings (see Two-AI setup). Chat replies to the user follow the Communication Style section — that rule is about the PROJECT, not the conversation.
- Date format: always `en-GB` (DD/MM/YYYY). Never `en-US`.
- Never hardcode `amber-*` — use `accent-*`.
- Comments only when WHY is non-obvious.
- Component > ~150 lines → split it.
- No error handling for impossible scenarios. Only validate at system boundaries.
- **Never add a speculative external-API field.** Verify a GraphQL/REST shape live (introspection, one real sample) before shipping code that reads it — the EnTur `nearest` query, the DSLD endpoints and the Hevy enum coverage were all confirmed this way; the still-open Kassalapp label values and the SpO2 shape are flagged as unconfirmed precisely because they weren't.

---

## Key Patterns

**Toast store:** `import { toast } from '../../../app/store'`

**Tasks — one table for every domain.** `tasks` is the single source of truth for tasks across every page; **never create a per-feature task table.** Enums: `domain` `personal|work|media` · `section` `inbox|today|tomorrow|this_week|backlog` · `status` `open|in_progress|done|cancelled` · `priority` `low|medium|high` · **`source_type` `manual|movie|tv_series|media|calendar|ai|training_session|project_item`** (widened by migrations 039 and 047). Canonical queries: Work board `domain='work' AND status != 'cancelled'` · Today `section='today' OR (due_date = current_date AND status='open')` · media plans `domain='media' AND source_type='media'` · inbox `section='inbox' AND status='open'`.

**`tasks.start_date` (migration `069`) is `due_date`'s OPENING edge**, not a second deadline: `start_date=A` + `due_date=B` means "do this between A and B", and `due_date` stays the **sole** deadline representation — so overdue, the brief, the push, `phone-gateway` and `workMeta.ts` learn no new concept. `ToDoItem` renders **one** range chip *instead of* the due chip (never both — that row's own comment records a real 136px-of-393px title-starvation bug) and still takes its colour and ⚠ from `due_date`. `TasksPanel` gains an **"Open now"** group (`start_date ≤ today < due_date`), computed client-side off `fetchAllTasks`' existing `select('*')`, so before 069 `start_date` simply reads `undefined` and the group stays empty — no probe, no query. The window's last day belongs to Today (on `due_date` the deadline is what matters), and Open now only ever takes rows *from* Upcoming, so nothing renders twice. **A window predicate is NEVER added to `fetchTasksForDay`** — a `42703` there takes down `DayView`, `DayQuickRail`, `HomePage` and Home's `TodaySummary` at once. Written on **both** `UnifiedPlanModal` save paths via `TaskWindowField` (season chips reusing `windowChips`' math; it could not live in `taskExtra`, which is a `React.ReactNode` with no access to `form`/`patch`), and `tasksApi` retries the write without `start_date` on `42703`/`PGRST204` (the `recipes.fiber_g` precedent) while create omits the key entirely when unset. Fixed in the same pass: `CreateTaskInput` had no `description`, so the Notes textarea was **silently dropped on create** while edit persisted it.

**Planning — `UnifiedPlanModal`:** The ONE modal for all task/time-block planning (`src/shared/components/plan-modal/`). Schedule + Task tabs, shared editable title, always-on-top z-index. Shape it entirely from the call site — never edit the modal folder:
- `config` → `tabs`, `defaultTab`, `heading`, `hide*/lock*` field keys per tab
- `defaults` → prefills (title/date/startTime/duration/category/color/section/priority/domain/**startDate**/dueDate…)
- `source` → `{ sourceType, sourceId, taskSourceType }`; `sourceType` MUST be a valid `time_blocks.source_type` (`task`/`training_session`/`movie`/`tv_episode`/`project_item`/`calendar`/`manual`)
- `scheduleExtra` / `taskExtra` → caller-owned children injected into a tab
- `task` → edit mode (Task tab) · `onSaved` → post-save hook
- Rules + changelog live at the top of `UnifiedPlanModal.tsx`; log every logic change there.

**RP5 Games:** Separate Supabase instance (`VITE_RP5_SUPABASE_URL`). Read from `v_games_summary` / `v_games_full` views. Write to the raw `games` table. `series_name` only exists in the view — never select it from raw `games`.

**Home widgets:** All use `WidgetShell` + `useWidgetState`. Disable queries when collapsed.

**Daily briefing (Home):** `DailyBriefing.tsx` — an AI-written Turkish morning digest. `briefingApi.ts::generateDailyBriefing()` gathers real data (today's tasks/schedule, upcoming + last training, weekly currency trend via `fetchCurrencyWeekTrend`, weather, TR/NO/world news headlines, media in progress, in-progress project items) into a context string and calls the shared `invokeAI` with a briefing-specific prompt (plain text, emoji section headers, NO markdown — rendered with `whitespace-pre-wrap`, no markdown lib). **Once-per-day** is enforced in `useDailyBriefing.ts`: React Query keyed by the date + `staleTime: Infinity` + `initialData` seeded from a localStorage cache (`lasci.dailyBriefing`, `{date,text}`) when the cached entry is for today; the ↻ button is the explicit manual regenerate (costs one AI request). localStorage (not a DB table) chosen deliberately: zero migration, single-user, per-browser is fine.

**TMDB images:** `https://image.tmdb.org/t/p/{size}{path}` (e.g. `w342`)

**Google Calendar + Tasks:** ONE unified "Connect Google" consent — the scope list in `SettingsMenu.tsx` is `calendar.events` (read + write), `tasks`, and the three `googlehealth.*.readonly` scopes, all on one refresh token. Token stored in `useCalendarStore`; a reconnect is required if previously connected with a narrower scope set.
- **Two-way block ↔ event link (migration `038`):** `time_blocks.google_calendar_event_id` stores the created event id; `scheduleApi.updateTimeBlock`/`deleteTimeBlock` (at the API layer, so ALL paths — modal, agenda edits, task delete — are covered) sync/remove the calendar event when a block moves or is deleted. All calendar calls are best-effort (try/catch + `logError`) — a sync failure never blocks the local write.
- **One task = ONE Google entry.** A task that will also become a calendar event used to produce a Google **Task** *and* an event — two things in Google for one thing here. Fixed: `useCreateTask` takes `skipGoogleTasks`, which `UnifiedPlanModal` passes whenever the task will be linked to an event; `linkCalendarEvent` is **idempotent** (re-reads the block and early-returns when `google_calendar_event_id` already exists, so no orphaned/duplicate events); and `syncTaskBlock` deletes a leftover Google Task once an event is linked (retroactive cleanup on the next edit). "Add to Google Calendar" now also appears in **edit** mode, rendered as a disabled "Added ✓" readout when already linked. **Don't "simplify away" the `skipGoogleTasks` branch in `useTodos.ts`** — it's what prevents the duplicate.

**Schedule cache invalidation:** time-block mutations (`useCreateTimeBlock`/`useUpdateTimeBlock`/`useDeleteTimeBlock`) invalidate the WHOLE `['schedule']` namespace + `['calendar']`, not a single day key — consumers read schedule under different sub-keys (`['schedule','day',date]` for Daily/Home/Work via the shared `useTimeBlocks`, `['schedule','training-range',…]` for the Training calendar / Home next-session). **Never give a schedule view its own private query key** (Work's old `['time-blocks']` key went stale because nothing invalidated it). Same class of bug bit the food card — see Food's cache-key note.

**@headlessui/react v2 — UI primitives (never use manual open/close state for these):**

| Use case | Component | Anchor / notes |
|---|---|---|
| Modal / drawer | `Dialog` + `DialogBackdrop` + `DialogPanel` | `transition` prop on both; `data-[closed]:` for exit animation |
| Search autocomplete | `Combobox` + `ComboboxInput` + `ComboboxOptions` + `ComboboxOption` | `immediate` if options should show on focus; `data-[focus]:bg-cream-50` on option |
| Floating action panel | `Popover` + `PopoverButton` + `PopoverPanel` | `anchor="top start"` / `anchor="bottom start"` via Floating UI |
| Dropdown menu | `Menu` + `MenuButton` + `MenuItems` + `MenuItem` | `anchor="bottom end"`; keyboard nav built-in |

All headlessui components handle click-outside close, Escape, focus trap, portal rendering and ARIA. Never add these manually.

Reference files: `src/shared/components/plan-modal/UnifiedPlanModal.tsx` (Dialog), `src/features/home/components/ruter/StopSearchInput.tsx` (Combobox), `src/features/media/components/PlanThisButton.tsx` (Popover), `src/shared/components/SettingsMenu.tsx` (Menu).

---

## Edge Functions (Supabase)

**Edge Functions are deployed MANUALLY** (Supabase Dashboard, or `supabase functions deploy` via CLI). A GitHub Actions auto-deploy job was tried and removed — it kept failing on the runner and wasn't worth the noise. Manual deploy is clean because **every function is self-contained (no `_shared/` imports)** — the shared Hevy upsert logic is inlined into all 4 Hevy functions, so a Dashboard paste no longer hits the "Module not found `_shared`" bundling error. **DB migrations are also manual** (Dashboard → SQL Editor or `supabase db push`); they do NOT run in GitHub Actions.

**`verify_jwt = false` applies to SIX functions** — `supabase/config.toml` records them and a CLI deploy applies it automatically; a **Dashboard** deploy needs the "Enforce JWT Verification" toggle set by hand for each: **`hevy-sync`, `health-export-webhook`, `google-health-sync`, `ai-proxy`, `phone-gateway`, `push-send`.** Every one of them authenticates with its own Bearer/custom secret rather than a Supabase JWT (checked in-code), so leaving platform verification ON makes Supabase reject the call **before our code runs**. This is a real production gotcha, hit twice.

**Correct-in-the-repo ≠ deployed.** Because functions and migrations are manual, a merged commit can still be running old code in production. Sequence any "must be deployed before X" work against the moment the *user-triggerable* path goes live (a Fetch-now button, a new gateway action) — **not** against a cron being enabled; a single button click can write real rows immediately.

| Function | Purpose |
|---|---|
| `ai-proxy` | Gemini AI calls — the generic DB tool layer, special-purpose tools, vision, semantic search, model fallback chain, structured (`responseSchema`) and `fetchUrl` modes. Dual-auth: browser user JWT (validated in-code via `getUser`) OR the iPhone device secret (`x-phone-secret`). Full detail: the AI section above; cost/capability reference: `docs/ai-cost-capability-analysis.md`. |
| `phone-gateway` | The iPhone's single DURABLE entry point (Shortcuts / Scriptable) — authenticates by `x-phone-secret` (== `PHONE_GATEWAY_SECRET`), acts as the single user (`HEVY_USER_ID`) via the service role **server-side** (the service key never touches the device), takes a flat `{action, …}` body (no nested JSON, no expiring JWT). 8 deterministic actions run directly; 3 AI actions forward to `ai-proxy` with the same secret. Full action list + the ~25s latency design: the iPhone section above. Setup + contract: `docs/iphone-examples.md`; rationale: `docs/iphone-web-integration.md`; widgets: `docs/scriptable-widgets.md`; food logger: `docs/scriptable-food-logger.md`. |
| `push-send` | Web Push sender — signs a VAPID push to every `push_subscriptions` row so a notification reaches the lock screen while the PWA is closed. Dual-auth: the pg_cron `lascis-morning-push` job (`x-cron-secret` == `PUSH_CRON_SECRET`, acts as `HEVY_USER_ID`, `{trigger:'morning'}` → a short deterministic tasks+training summary) OR a browser JWT (`{title,body,url}` ad-hoc). Prunes expired subscriptions on 404/410. Migration `068`. Runbook: `docs/web-push-setup.md`. |
| `food-search` | Kassalapp (kassal.app) proxy — Norwegian branded groceries by name (`{search}`) or barcode (`{ean}`), `KASSALAPP_API_KEY` kept server-side. Returns per-100g `BarcodeProduct`s, or `[]` if the key is unset (the client then falls back to Open Food Facts, which it calls direct — CORS-open, no key). `verify_jwt` ON. |
| `google-health-sync` | Fitbit Air poller — Google Health API v4 → `health_metrics` (`source_family='fitbit'`, hourly grain) + `health_sleep_segments`. Written against the live-verified surface in `docs/google-health-api-surface.md` (**three casings, one API**: kebab-case dataType path ids, snake_case filter tokens, camelCase JSON; numeric values arrive as STRINGS). **Three locked gates:** (1) the intraday `dataPoints` **list** endpoint, never `:rollUp`/`:dailyRollUp`, for cumulative metrics — a pre-aggregated daily number cannot feed the hourly bucket-merge the resolver depends on; (2) a **platform allowlist** — `dataPoint.dataSource.platform === 'FITBIT'` passes, everything else (HEALTH_KIT, missing, unknown future values) is dropped and counted in `skipped_non_fitbit`. The Google Health iOS app holds Apple HealthKit READ permission and republishes it into this same API, so unfiltered ingest would double-store Apple data as `fitbit` — and a missed platform filter produces wrong numbers **that look like real Fitbit data**; default-deny matches this repo's convention; (3) hourly aggregation at ingest (HR samples → per-hour Min/Avg/Max, matching the Apple `minmaxavg` value shape). **v1 metrics:** steps, active energy, heart rate, sleep (stages → `health_sleep_segments` + one aggregate `sleep_analysis`-shaped row, LIGHT→core), resting HR. **v1.1 (shipped 22/07/2026):** distance (mm/m → km via `distanceKm`) and active zone minutes as hourly cumulative sums with their own unit normalisation, HRV (`daily-heart-rate-variability`) and skin-temperature deviation as daily rows, and SpO2 via a **dual-path A/B** — `oxygen-saturation` samples → hourly Min/Avg/Max, else the `daily-oxygen-saturation` summary — with an `oxygen_saturation_source` counter in the response, because the shape the Air populates is still **unconfirmed**. **v1.2 completeness fix (20/08/2026):** a fresh discovery-doc re-verification found `distance`/HRV/skin-temperature were all reading WRONG field names (shipped as unverified guesses in v1.1, confirmed wrong once checked — zero Fitbit rows for all three despite the ingestion calls existing) and `active_zone_minutes` was summing raw per-zone minutes with no weighting (Fitbit counts Cardio/Peak zones ×2). Also added `basal_energy_burned` and `respiratory_rate` (never fetched at all despite being documented as shipped) and `vo2_max` (`daily-vo2-max`, new). Full field-name detail: `docs/fitbit-air-integration.md` §6. **Per-metric failure isolation (real incident, fixed same day):** every data-type fetch used to share ONE try/catch, so a single transient Google-side error (a plain HTTP 500 or a `DEADLINE_EXCEEDED` timeout — confirmed live in `app_error_logs`) on ANY one metric discarded every row already collected for every OTHER metric in that run, with nothing ever surfacing to the user (cron failures only ever reached `app_error_logs`, never a toast) — this held the DB **13 real days without a single new Fitbit row** (8–20 Aug 2026) despite the cron still firing. Each metric block is now wrapped in its own `guarded()` helper: one failure is recorded (into the response's `partial_errors` and a `partial:true` `app_error_logs` row) and every other metric still saves. `last_success_at` is set whenever the run reaches the upsert step at all, not only on a fully clean run — a partially-successful sync is still progress and must not look identical to a total outage. Dual auth: user JWT ("⟳ Fitbit" in Settings ⚙ → Google) or `x-sync-secret` (cron). Writes `google_health_sync_state` (migration `063`) for reconnect/stale cues; `invalid_grant` → `reconnect_required`. **Cadence:** Air→phone→Google-cloud latency dominates (~15–30 min), so sub-15-min polling gains nothing — recommended cron `0 * * * *`; quota (300 req/min/user, ~6 req/run) is never the constraint. **Query-range cap is 14 days** for HR / active-minutes / total-calories, so an outage longer than that is unrecoverable. |
| `calendar-oauth` / `calendar-token` / `calendar-disconnect` | Google OAuth — **one unified "Connect Google" consent**: Calendar + Tasks + the three `googlehealth.*.readonly` scopes on ONE refresh token in `user_calendar_tokens` (scope list lives in `SettingsMenu.tsx`; adding a future scope = append there + one incremental re-consent). Health access tokens are minted by **down-scoping at refresh time** (only the 3 health scopes on the refresh request) because **the Google Health API rejects mixed-scope tokens** — `403 DISALLOWED_OAUTH_SCOPES` naming `cl_events`,`tasks`. `calendar-token` is deliberately left wide. The design-time `google-health-oauth`/`google-health-token`/`google-health-webhook` functions and a `user_health_tokens` table **were never built — do not look for them.** |
| `news-proxy` | RSS feed proxy |
| `strava-auth` / `strava-activities` / `strava-disconnect` | Strava OAuth + activity fetch |
| `hevy-initial-sync` | Bulk import all Hevy data (workouts, templates, routines, body measurements) |
| `hevy-sync` | Webhook receiver — new workout from Hevy → upsert. Hevy sends its own bearer secret, so **JWT verification must be OFF** (see the six-function note above); the symptom when it's on is "manual Sync works, nothing arrives automatically". |
| `hevy-incremental-sync` | Events-based incremental sync (workout edits/deletes since the last cursor) + a full re-fetch/upsert of routines, routine folders and body measurements (event-less collections), so Sync surfaces new Hevy-app routines without a full re-import |
| `hevy-api` | Write proxy — web → Hevy (create/update workouts, routines, body measurements). Also the single place the documented Hevy payload traps are handled server-side. |
| `health-export-webhook` | Webhook receiver — Health Auto Export POSTs `{data:{workouts,metrics}}` → upsert into `health_workouts`/`health_metrics`, stamping `source_family='apple'`. Bearer secret, JWT verification OFF. Every incoming metric point becomes its own row, idempotent on `(user_id,metric_name,recorded_at,source)` / workout `id`, with no aggregation at ingest — which is what makes it safe against "Batch Requests" splitting one export into multiple calls in any order. |

**Hevy upsert logic (`upsertWorkoutToDb`/`upsertRoutineToDb`)** — upsert row → delete exercises → re-insert exercises+sets, plus the "a logged Hevy workout auto-closes its planned-session task" behaviour (if `workout.routine_id` matches an open `tasks` row with `source_type='training_session'`, that task + its linked `time_blocks` row are deleted). This was a shared `_shared/hevySync.ts` Deno module, but **Supabase Dashboard deploys don't bundle sibling `_shared/` files** (deploy-time "Module not found"), so it is **inlined verbatim into all 4 Hevy functions** (`hevy-sync`/`hevy-initial-sync`/`hevy-incremental-sync`/`hevy-api`) — each self-contained and deployable by any method. **Trade-off: if this logic changes, update all four copies by hand** (a header comment in each flags this). There is no `supabase/functions/_shared/` directory.

### Pending manual steps (deploy checklist)

One list instead of per-feature notes, because otherwise it's impossible to tell what's still outstanding. Everything below is applied by the **user**, not by CI.

| Step | Needed for |
|---|---|
| Apply migrations in `supabase/migrations/` in order (Dashboard → SQL Editor or `supabase db push`) | Any schema change. Client code is written to survive an un-applied migration where noted (Food's `status`/`fiber_g`/`is_temp`/`image_url` fallbacks; AI's best-effort logging writes are no-ops until 064/065). |
| Apply **`069_wish_items.sql`** (`wish_items` + `tasks.start_date`; **updates no existing row**) | Wishes + task windows. Un-applied is survivable: wish reads return `[]`, wish writes throw a named error, `tasks.start_date` writes retry without the column, and `TasksPanel`'s "Open now" stays empty. **`phone-gateway` is NOT redeployed for this feature.** |
| Apply **`070_athlete_profile.sql`** (`athlete_profile` + `athlete_limitations`; **updates no existing row**) | Training's athlete profile + limitations. Un-applied is survivable: every read degrades to `null`/`[]`, every write throws a named "migration 070 not applied" error, `ptCoachApi`'s `PROFİL:` block simply never appears. **`phone-gateway` is NOT redeployed for this feature.** |
| Redeploy `ai-proxy` whenever `DB_CATALOG`, `TOOLS` or `SYSTEM_PROMPT` change server-side | The catalog and tool declarations live in the function, not the client. Outstanding: `wish_items` (`rw` + `EMBED_SOURCES`) and `tasks.start_date`, and `athlete_profile`/`athlete_limitations` (`rw`) + the new `get_athlete_profile` tool. JWT enforcement stays **OFF**. |
| Redeploy `push-send` — **only after confirming Web Push actually delivers** (068 applied, `VAPID_*` + `PUSH_CRON_SECRET` in Vault, Settings ⚙ → Notifications tested end-to-end) | The morning brief's once-per-season "period starts today" segment. Web Push being live is **UNVERIFIED**; if it isn't, this is a prerequisite, not a nice-to-have. JWT **OFF**. |
| Vault secrets: `GEMINI_API_KEY`, `HEVY_*`, `HEALTH_EXPORT_WEBHOOK_SECRET`, `GOOGLE_HEALTH_SYNC_SECRET`, `GOOGLE_CLIENT_ID/SECRET`, `STRAVA_CLIENT_ID/SECRET`, `KASSALAPP_API_KEY`, `PHONE_GATEWAY_SECRET`, `VAPID_*`, `PUSH_CRON_SECRET` | See Environment Variables below. |
| Set "Enforce JWT Verification" OFF for the six functions listed above (Dashboard deploys only) | Hevy/HAE webhooks, Google Health cron, phone gateway, push cron. |
| `VITE_VAPID_PUBLIC_KEY` as a GitHub Actions **Variable** + apply `068` + deploy `push-send` | Web Push. |
| Supabase Dashboard → Authentication → URL Configuration → Redirect URLs must include `#/reset-password` (Pages URL + localhost) | Password recovery. |
| Health Auto Export app config: Export Version 2, Summarize ON, Time Grouping **Hours**, all metrics, Since Last Sync (+ weekly reconciliation) | Apple Health ingest correctness. |
| Google Cloud consent screen published to **Production** | Non-expiring refresh token for background Google Health sync. |

---

## Environment Variables

Client vars are baked at build time by `.github/workflows/deploy.yml`; everything else lives in **Supabase Vault only** and is read by an edge function at runtime. No AI or provider key ever reaches client code.

| Variable | Where |
|---|---|
| `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` | Client build — main Supabase |
| `VITE_TMDB_API_KEY` | Client build — TMDB (client-safe) |
| `VITE_GOOGLE_CLIENT_ID` | Client build — Google OAuth (Calendar + Tasks + Health consent) |
| `VITE_STRAVA_CLIENT_ID` | Client build — Strava OAuth start |
| `VITE_OXR_APP_ID` | Client build — Open Exchange Rates (Currency widget + the daily briefing's week trend) |
| `VITE_RP5_SUPABASE_URL` / `VITE_RP5_SUPABASE_ANON_KEY` | Client build — RP5 games Supabase (separate instance) |
| `VITE_VAPID_PUBLIC_KEY` | Client build (GitHub Actions **Variable**) — the VAPID **public** key the browser uses to subscribe to Web Push. Safe to expose. |
| `GEMINI_API_KEY` | Vault — the ONLY AI key; read by `ai-proxy`. (There is no Claude/OpenAI key in this project.) |
| `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` | Vault — server side of the unified Google OAuth (`calendar-oauth`/`calendar-token`, and the down-scoped refresh for `google-health-sync`) |
| `STRAVA_CLIENT_ID` / `STRAVA_CLIENT_SECRET` | Vault — `strava-auth`/`strava-activities` |
| `HEVY_API_KEY` / `HEVY_WEBHOOK_SECRET` / `HEVY_USER_ID` | Vault — Hevy API + webhook auth; `HEVY_USER_ID` is the single-user id reused by every server-side path (health webhook, phone gateway, push cron) |
| `HEALTH_EXPORT_WEBHOOK_SECRET` | Vault — Health Auto Export sends it as `Authorization: Bearer <secret>` |
| `GOOGLE_HEALTH_SYNC_SECRET` | Vault — `google-health-sync`'s cron-path auth (`x-sync-secret`). The in-app "Fetch now" path uses the user JWT instead. |
| `KASSALAPP_API_KEY` | Vault — personal kassal.app token, read by `food-search`. The client never sees it. |
| `PHONE_GATEWAY_SECRET` | Vault — the iPhone's durable device secret (`x-phone-secret`). Authenticates `phone-gateway` AND `ai-proxy`'s phone branch; both act as `HEVY_USER_ID` server-side. Never put a login/service key on the device. Revoke = rotate this value. `docs/iphone-examples.md`. |
| `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` | Vault — Web Push (VAPID) signing keys for `push-send`. Generate with `npx web-push generate-vapid-keys`. `docs/web-push-setup.md`. |
| `PUSH_CRON_SECRET` | Vault — the pg_cron morning-push job's auth (`x-cron-secret` → `push-send`). Read from Vault **inside** the cron (migration 068), never written into the SQL text. |

`SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` are **injected into every edge function by the platform** — don't add them to Vault and never expose the service-role key to a client or a device.
