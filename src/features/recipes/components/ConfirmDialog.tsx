// The Food copy of the confirm dialog was identical to the shared one; kept as
// a re-export so existing imports keep working. New code: `await modal.confirm()`.
export { ConfirmDialog } from '../../../shared/components/ConfirmDialog'
